# Cài đặt CircleCI trên Windows, macOS, Linux, Docker, Kubernetes

## Metadata

- **URL:** https://devops.vn/posts/cai-dat-circleci/
- **Title:** Cài đặt CircleCI trên Windows, macOS, Linux, Docker, Kubernetes
- **OG Title:** Cài đặt CircleCI trên Windows, macOS, Linux, Docker, Kubernetes
- **H1:** Cài đặt CircleCI trên Windows, macOS, Linux, Docker, Kubernetes
- **Meta Description:** CircleCI Runner là thành phần cho phép bạn chạy job trên hạ tầng riêng, tương tự Jenkins Agent.
- **Content scope:** dv-post-content

## Headings

  - (H2) 1. Tổng quan
  - (H2) 2. Prerequisites
    - (H3) Yêu cầu hệ thống
    - (H3) Phần cứng
  - (H2) 3. Cài đặt CircleCI Runner bằng Docker
    - (H3) 3.1 Docker Compose
    - (H3) 3.2 Khởi chạy
  - (H2) 4. Cài đặt Runner trên Windows
    - (H3) 4.1 Các bước
    - (H3) 4.2 Lưu ý
  - (H2) 5. Cài đặt trên macOS
    - (H3) 5.1 Cài đặt
    - (H3) 5.2 Quản lý
  - (H2) 6. Cài đặt Runner trên Linux
    - (H3) 6.1 Ubuntu/Debian
    - (H3) 6.2 RHEL / CentOS / Rocky
  - (H2) 7. Triển khai CircleCI Runner trên Kubernetes
    - (H3) circleci-k8s.yaml
  - (H2) 8. Nginx caching proxy
  - (H2) 9. Executor Types
    - (H3) 9.1 Machine Runner
    - (H3) 9.2 Container Runner
  - (H2) 10. Post-Installation Checklist

## Content

## 1. Tổng quan

[CircleCI](https://circleci.com/) Runner là thành phần cho phép bạn chạy job trên hạ tầng riêng, tương tự Jenkins Agent.
CircleCI Cloud chịu trách nhiệm điều phối workflow, còn Runner chỉ tập trung thực thi.

- **CircleCI Cloud**: UI, pipeline orchestration, log & artifact.
- **Self-Hosted Runner**: chạy on-premise / VM / container / Kubernetes.

Runner phù hợp cho workload yêu cầu bảo mật, build [Docker](https://devops.vn/tag/Docker/) nặng, GPU, hoặc môi trường đặc thù mà CircleCI cloud không hỗ trợ.

![CircleCI](https://devops.vn/uploads/images/2025/12/3d83c415-cd6e-42bd-90fa-c26077208800.webp)

## 2. Prerequisites

### Yêu cầu hệ thống

- **Resource Class Token** (lấy tại CircleCI => Project => Settings => Runner => Inventory).
- Đã tạo **namespace** cho runner.

### Phần cứng

- Tối thiểu: **1 vCPU**, **2GB RAM**
- Khuyến nghị: **2 vCPU+, 4–8GB RAM** nếu chạy Docker build hoặc test nhiều dependency.

## 3. Cài đặt CircleCI Runner bằng Docker

### 3.1 Docker Compose

```
version: '3.8'

services:
  circleci-runner:
    image: circleci/runner:launch-agent
    container_name: circleci-runner
    restart: unless-stopped
    environment:
      - CIRCLECI_RESOURCE_CLASS=my-namespace/my-docker-runner
      - CIRCLECI_API_TOKEN=your-runner-token
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - ci-net

networks:
  ci-net:
```

**Lưu ý bảo mật:** 

Mount **docker.sock** cho phép runner có toàn quyền root trên host.
Nếu dùng trong production, cần hạn chế quyền hoặc chạy runner trong máy ảo tách biệt.

### 3.2 Khởi chạy

```
docker-compose up -d
docker logs -f circleci-runner
```

Runner sẽ tự kết nối outbound lên CircleCI Cloud.

## 4. Cài đặt Runner trên Windows

### 4.1 Các bước

1. Tải binary **Launch Agent** dành cho Windows.
2. Tạo thư mục:
`C:\Program Files\CircleCI Runner
`
3. Tạo file:
`launch-agent-config.yaml
`

Ví dụ:

```
runner:
  name: "windows-node"
  token: "your-token"
  resource_class: "namespace/windows"
  mode: single-task
```

1. Cài đặt service (NSSM hoặc Windows Service).
2. Khởi chạy Runner.

Logs có thể xem tại **Event Viewer**.

### 4.2 Lưu ý

- Cần cài Git, MSBuild, Chocolatey trước.
- Job Windows thường dùng shell: `powershell.exe` hoặc `cmd.exe`.

## 5. Cài đặt trên macOS

### 5.1 Cài đặt

```
curl -fLSs https://circleci-binary-releases.s3.amazonaws.com/circleci-launch-agent/release/latest/circleci-launch-agent-darwin-amd64 > circleci-launch-agent
chmod +x circleci-launch-agent
```

### 5.2 Quản lý

- Chạy thủ công:
`./circleci-launch-agent --config launch-agent-config.yaml
`
- Nếu muốn chạy background => tạo Launchd service.

## 6. Cài đặt Runner trên Linux

### 6.1 Ubuntu/Debian

```
curl -fLSs https://circleci-binary-releases.s3.amazonaws.com/circleci-launch-agent/release/latest/circleci-launch-agent-linux-amd64 > circleci-launch-agent
chmod +x circleci-launch-agent
sudo mv circleci-launch-agent /usr/local/bin/
```

Tạo user:

```
sudo useradd --comment 'CircleCI Runner' --create-home circleci
sudo mkdir -p /etc/circleci-runner
sudo chown -R circleci: /etc/circleci-runner
```

**Thêm systemd service**:

```
[Unit]
Description=CircleCI Runner
After=network.target

[Service]
User=circleci
ExecStart=/usr/local/bin/circleci-launch-agent --config /etc/circleci-runner/launch-agent-config.yaml
Restart=always

[Install]
WantedBy=multi-user.target
```

### 6.2 RHEL / CentOS / Rocky

```
sudo dnf install git -y
curl -fLSs https://circleci-binary-releases.s3.amazonaws.com/circleci-launch-agent/release/latest/circleci-launch-agent-linux-amd64 > circleci-launch-agent
sudo chmod +x circleci-launch-agent
sudo mv circleci-launch-agent /bin/
```

## 7. Triển khai CircleCI Runner trên Kubernetes

### circleci-k8s.yaml

```
apiVersion: v1
kind: Namespace
metadata:
  name: circleci
---
apiVersion: v1
kind: Secret
metadata:
  name: circleci-runner-secret
  namespace: circleci
stringData:
  CIRCLECI_API_TOKEN: "your-token-here"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: circleci-runner
  namespace: circleci
spec:
  replicas: 1
  selector:
    matchLabels:
      app: circleci-runner
  template:
    metadata:
      labels:
        app: circleci-runner
    spec:
      containers:
      - name: runner
        image: circleci/runner:launch-agent
        env:
        - name: CIRCLECI_RESOURCE_CLASS
          value: "my-namespace/k8s-runner"
        - name: CIRCLECI_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: circleci-runner-secret
              key: CIRCLECI_API_TOKEN
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
```

**Lưu ý quan trọng:** 

CircleCI Runner trên Kubernetes *không tự build Docker* nếu không có Docker-in-Docker hoặc buildkit sidecar.

## 8. Nginx caching proxy

CircleCI Runner không có giao diện web, nên Reverse Proxy là không cần thiết.
Tuy nhiên, bạn có thể dùng Nginx làm caching proxy để tăng tốc tải dependency (npm, pip…).

```
server {
    listen 8080;
    server_name cache.local;

    location / {
        proxy_pass https://registry.npmjs.org;
        proxy_set_header Host registry.npmjs.org;

        proxy_cache my_cache;
        proxy_cache_valid 200 1d;
        proxy_ignore_headers Set-Cookie;
    }
}
```

## 9. Executor Types

### 9.1 Machine Runner

- Chạy trực tiếp trên OS.
- Dùng cho workload GPU, privileged, kernel-level, hoặc build Docker nặng.

### 9.2 Container Runner

- Mỗi job chạy trong container mới.
- Clean, reproducible, dễ scale.

## 10. Post-Installation Checklist

- Kiểm tra runner tại **CircleCI => Inventory => Runners**
- Tạo `.circleci/config.yml`
- Khớp `resource_class` với runner
- Push pipeline đầu tiên để validate

Ví dụ cấu hình:

```
jobs:
  build:
    machine: true
    resource_class: my-namespace/my-runner
    steps:
      - checkout
      - run: echo "Running on self-hosted CircleCI Runner"
```

## Highlight Text

- CircleCI Cloud
- Self-Hosted Runner
- Resource Class Token
- namespace
- 1 vCPU
- 2GB RAM
- 2 vCPU+, 4–8GB RAM
- Lưu ý bảo mật:
- docker.sock
- Launch Agent
- Event Viewer
- Thêm systemd service
- Lưu ý quan trọng:
- không tự build Docker
- CircleCI => Inventory => Runners

## Images

- ![CircleCI](https://devops.vn/uploads/images/2025/12/3d83c415-cd6e-42bd-90fa-c26077208800.webp)  — alt: `CircleCI`

## Code Blocks

```
version: '3.8'

services:
  circleci-runner:
    image: circleci/runner:launch-agent
    container_name: circleci-runner
    restart: unless-stopped
    environment:
      - CIRCLECI_RESOURCE_CLASS=my-namespace/my-docker-runner
      - CIRCLECI_API_TOKEN=your-runner-token
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - ci-net

networks:
  ci-net:
```

```
docker-compose up -d
docker logs -f circleci-runner
```

```
C:\Program Files\CircleCI Runner
```

```
launch-agent-config.yaml
```

```
runner:
  name: "windows-node"
  token: "your-token"
  resource_class: "namespace/windows"
  mode: single-task
```

```
curl -fLSs https://circleci-binary-releases.s3.amazonaws.com/circleci-launch-agent/release/latest/circleci-launch-agent-darwin-amd64 > circleci-launch-agent
chmod +x circleci-launch-agent
```

```
./circleci-launch-agent --config launch-agent-config.yaml
```

```
curl -fLSs https://circleci-binary-releases.s3.amazonaws.com/circleci-launch-agent/release/latest/circleci-launch-agent-linux-amd64 > circleci-launch-agent
chmod +x circleci-launch-agent
sudo mv circleci-launch-agent /usr/local/bin/
```

```
sudo useradd --comment 'CircleCI Runner' --create-home circleci
sudo mkdir -p /etc/circleci-runner
sudo chown -R circleci: /etc/circleci-runner
```

```
[Unit]
Description=CircleCI Runner
After=network.target

[Service]
User=circleci
ExecStart=/usr/local/bin/circleci-launch-agent --config /etc/circleci-runner/launch-agent-config.yaml
Restart=always

[Install]
WantedBy=multi-user.target
```

```
sudo dnf install git -y
curl -fLSs https://circleci-binary-releases.s3.amazonaws.com/circleci-launch-agent/release/latest/circleci-launch-agent-linux-amd64 > circleci-launch-agent
sudo chmod +x circleci-launch-agent
sudo mv circleci-launch-agent /bin/
```

```
apiVersion: v1
kind: Namespace
metadata:
  name: circleci
---
apiVersion: v1
kind: Secret
metadata:
  name: circleci-runner-secret
  namespace: circleci
stringData:
  CIRCLECI_API_TOKEN: "your-token-here"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: circleci-runner
  namespace: circleci
spec:
  replicas: 1
  selector:
    matchLabels:
      app: circleci-runner
  template:
    metadata:
      labels:
        app: circleci-runner
    spec:
      containers:
      - name: runner
        image: circleci/runner:launch-agent
        env:
        - name: CIRCLECI_RESOURCE_CLASS
          value: "my-namespace/k8s-runner"
        - name: CIRCLECI_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: circleci-runner-secret
              key: CIRCLECI_API_TOKEN
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
```

```
server {
    listen 8080;
    server_name cache.local;

    location / {
        proxy_pass https://registry.npmjs.org;
        proxy_set_header Host registry.npmjs.org;

        proxy_cache my_cache;
        proxy_cache_valid 200 1d;
        proxy_ignore_headers Set-Cookie;
    }
}
```

```
jobs:
  build:
    machine: true
    resource_class: my-namespace/my-runner
    steps:
      - checkout
      - run: echo "Running on self-hosted CircleCI Runner"
```

## Tables

_(none)_

## Internal Links

- [https://devops.vn/expert-insights/ung-dung-ai-trong-devops-liam-nguyen/](https://devops.vn/expert-insights/ung-dung-ai-trong-devops-liam-nguyen/)
- [https://devops.vn](https://devops.vn)
- [Hỗ trợ?](https://devops.vn/support/)
- [Đăng nhập](https://devops.vn/login)
- [Đăng ký](https://devops.vn/signup)
- [Series](https://devops.vn/tag/devops-series/)
- [Mindset](https://devops.vn/tag/mindset/)
- [Linux](https://devops.vn/tag/linux/)
- [Bash](https://devops.vn/tag/bash/)
- [Docker](https://devops.vn/tag/docker/)
- [Kubernetes](https://devops.vn/tag/kubernetes/)
- [IaC](https://devops.vn/tag/infrastructure-as-code/)
- [Security](https://devops.vn/tag/security/)
- [Networking](https://devops.vn/tag/networking/)
- [Cloud](https://devops.vn/tag/cloud/)
- [Monitoring](https://devops.vn/tag/monitoring/)
- [Certificate](https://devops.vn/tag/certificate/)
- [AI](https://devops.vn/tag/ai/)
- [Tài nguyên](https://devops.vn/category/posts/)
- [Newsletter](https://devops.vn/newsletter/)
- [Giới thiệu](https://devops.vn/about/)
- [Blog](https://devops.vn/knowledge/)
- [Flashcards](https://devops.vn/flashcards/)
- [Cheat sheets](https://devops.vn/cheat-sheets/)
- [Sự kiện](https://devops.vn/events/)
- [Tin tức](https://devops.vn/news/)
- [Dành cho doanh nghiệp](https://devops.vn/vi/case-studies/)
- [Tiger Minh](https://devops.vn/author/minhtiger/)
- [09/01/2025](https://devops.vn/2025/01/09/)
- [Docker](https://devops.vn/tag/Docker/)
- [CircleCI](https://devops.vn/tag/circleci/)
- [macOS](https://devops.vn/tag/macos/)
- [Setup](https://devops.vn/tag/setup/)
- [Windows](https://devops.vn/tag/windows/)
- [https://devops.vn/event/devops-vietnam-x-linux-foundation/](https://devops.vn/event/devops-vietnam-x-linux-foundation/)
- [https://devops.vn/event/state-of-devops-vietnam-2026/](https://devops.vn/event/state-of-devops-vietnam-2026/)
- [https://devops.vn/new/gap-go-trinh-the-long-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-java/](https://devops.vn/new/gap-go-trinh-the-long-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-java/)
- [https://devops.vn/new/gap-go-tran-quoc-toan-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-reactjs/](https://devops.vn/new/gap-go-tran-quoc-toan-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-reactjs/)
- [https://devops.vn/new/gap-go-nguyen-phuc-bao-lam-vo-dich-dockerfile-contest-2025-docker-image-nho-nhat/](https://devops.vn/new/gap-go-nguyen-phuc-bao-lam-vo-dich-dockerfile-contest-2025-docker-image-nho-nhat/)
- [https://devops.vn/new/gap-go-nguyen-huu-phuong-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-reactjs/](https://devops.vn/new/gap-go-nguyen-huu-phuong-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-reactjs/)
- [https://devops.vn/new/gap-go-huynh-quang-tien-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-java/](https://devops.vn/new/gap-go-huynh-quang-tien-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-java/)
- [https://devops.vn/new/gap-go-nguyen-the-thanh-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-python/](https://devops.vn/new/gap-go-nguyen-the-thanh-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-python/)
- [https://devops.vn/new/gap-go-ngo-tan-tai-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-python/](https://devops.vn/new/gap-go-ngo-tan-tai-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-python/)
- [DevOps VietNam](https://devops.vn/)
- [https://devops.vn/feed](https://devops.vn/feed)
- [Hỗ trợ](https://devops.vn/support)
- [Ebooks](https://devops.vn/ebooks)
- [Điều khoản sử dụng](https://devops.vn/terms/)
- [Chính sách bảo mật](https://devops.vn/privacy/)
- [https://devops.vn/event/webinar-lo-trinh-tro-thanh-devops-engineer-thuc-te/](https://devops.vn/event/webinar-lo-trinh-tro-thanh-devops-engineer-thuc-te/)
- [https://devops.vn/posts/tai-sao-ufw-vo-tac-dung-voi-docker/](https://devops.vn/posts/tai-sao-ufw-vo-tac-dung-voi-docker/)
- [https://devops.vn/posts/memory-con-trong-nhung-kubernetes-container-van-oomkilled/](https://devops.vn/posts/memory-con-trong-nhung-kubernetes-container-van-oomkilled/)

## External Links

- [CircleCI](https://circleci.com/)
- [https://www.facebook.com/devopsvietnam.fanpage](https://www.facebook.com/devopsvietnam.fanpage)
- [https://www.tiktok.com/@devops.vn](https://www.tiktok.com/@devops.vn)
- [https://www.instagram.com/devops.vn/](https://www.instagram.com/devops.vn/)
- [https://www.linkedin.com/company/devopsvietnam](https://www.linkedin.com/company/devopsvietnam)
- [Email](mailto:support@devops.vn)
- [https://www.dmca.com/Protection/Status.aspx?ID=dd363dc3-580e-4a61-8f48-7b41fb97cf39](https://www.dmca.com/Protection/Status.aspx?ID=dd363dc3-580e-4a61-8f48-7b41fb97cf39)
- [https://transparencyreport.google.com/safe-browsing/search?url=devops.vn](https://transparencyreport.google.com/safe-browsing/search?url=devops.vn)
