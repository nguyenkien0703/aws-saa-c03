# Tạo và phân quyền người dùng trên Kubernetes

## Metadata

- **URL:** https://devopsvn.tech/kubernetes/tips/tao-va-phan-quyen-nguoi-dung-tren-kubernetes
- **Title:** Tạo và phân quyền người dùng trên Kubernetes

## Headings

- H1: Tạo và phân quyền người dùng trên Kubernetes
- H2: Vấn đề
- H2: Giải pháp
- H2: Kết luận
- H2: Bài viết liên quan

## Content

![Tạo và phân quyền người dùng trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/819ffec7-d947-4a94-92ad-c068b1e23e47/kubernetes-tips-rbac.drawio/w=1920,quality=90,fit=scale-down)

![Tạo và phân quyền người dùng trên Kubernetes](https://app.notion.com/icons/child_gray.svg)

# Tạo và phân quyền người dùng trên Kubernetes

Ngày đăng

Jun 5, 2023 3:38 AM

Tác giả

![](https://lh3.googleusercontent.com/a-/AOh14GgQbiQWTwvWr9UNj32QaIqsed4xULOmoWj58imk=s100)Quân Huỳnh

Hướng dẫn tạo và phân quyền người dùng với kubectl.

![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/ea5c2972-9a1d-4319-9588-3fbf37e59165/kubernetes-tips-rbac.drawio/w=1920,quality=90,fit=scale-down)

## Vấn đề

Hãy tưởng tượng ta gặp trường hợp như sau. Công ty ta có một Kubernetes Cluster với hai môi trường `pro` và `dev`. Với `pro` cho môi trường sản phẩm thực tế và `dev` dùng để phát triển sản phẩm. Trước giờ bạn chỉ làm một mình và có toàn bộ quyền để quản lý Kubernetes Cluster, mỗi lần Developer muốn làm gì đều phải thông qua bạn, kể cả xem logs. Sếp bạn thấy vậy hơi bất tiện nên kêu bạn tìm cách làm thế nào để mấy bạn Developer có thể tự liệt kê ứng dụng và xem logs ở dưới môi trường `dev`.

## Giải pháp

Ta dùng Role-based access control (RBAC) để giải quyết vấn đề trên. Các bước thực hiện như sau.

Tạo tệp tin `pod-logs-sa.yaml` cho Service Account:

Copy

```
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-logs
  namespace: dev
secrets:
  - name: pod-logs

---
apiVersion: v1
kind: Secret
metadata:
  name: pod-logs
  namespace: dev
  annotations:
    kubernetes.io/service-account.name: pod-logs
type: kubernetes.io/service-account-token
```

Copy

```
kubectl apply -f pod-logs-sa.yaml
```

Tạo tệp tin `pod-logs-role.yaml`:

Copy

```
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-logs
  namespace: dev
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log"]
  verbs: ["get", "list"]
```

Copy

```
kubectl apply -f pod-logs-role.yaml
```

Tạo RoleBinding:

Copy

```
kubectl -n dev create rolebinding pod-logs --serviceaccount=dev:pod-logs --role=pod-logs
```

Tiếp theo ta sẽ tạo người dùng và cho họ đăng nhập với Token lấy từ Service Account là `pod-logs`. Lấy Token:

Copy

```
kubectl -n dev describe secrets "$(kubectl -n dev describe serviceaccount pod-logs | grep -i Tokens | awk '{print $2}')" | grep token: | awk '{print $2}'
```

**Các bạn lưu ý chạy kubectl với namespace chính xác.** Trong ví dụ này là `-n dev`.

Gán giá trị trên vào biến `TOKEN`:

Copy

```
TOKEN=$(kubectl -n dev describe secrets "$(kubectl -n dev describe serviceaccount pod-logs | grep -i Tokens | awk '{print $2}')" | grep token: | awk '{print $2}')
```

Tạo người dùng với tên bất kì:

Copy

```
kubectl config set-credentials devopsvn --token=$TOKEN
```

Copy

```
User "devopsvn" set.
```

Chuyển Cluster hiện tại sang người dùng là `devopsvn`:

Copy

```
kubectl config set-context $(kubectl config current-context) --user=devopsvn
```

Tới bước này thì các bạn đã tạo người dùng thành công. Chạy các câu lệnh sau để kiểm tra quyền:

Copy

```
$ kubectl auth can-i get pods --namespace=dev
yes

$ kubectl auth can-i get pods --namespace=pro
no
```

Tiếp theo các bạn mở tệp tin `~/.kube/config` ra và xóa những người dùng khác và các thông tin không cần thiết, chỉ để lại người dùng `devopsvn`. Ví dụ:

Copy

```
apiVersion: v1
clusters:
- cluster:
    certificate-authority-data: ...
    server: ...
  name: devopsvn-cluster
contexts:
- context:
    cluster: devopsvn-cluster
    user: devopsvn
  name: devopsvn-cluster
kind: Config
preferences: {}
users:
- name: devopsvn
  user:
    token: ...
```

Sau đó ta gửi tệp tin này qua cho các bạn Developer.

## Kết luận

Hiện tại có rất nhiều giải pháp để ta có thể giải quyết vấn đề được đưa ra ở bài này, tuy nhiên các giải pháp khác lại hơi lằng nhằng và phức tạp. Đây là cách thủ công nhưng lại đơn giản và dễ làm nhất, phù hợp cho bạn mới. Các bạn có thể tham khảo giải pháp khác phức tạp hơn là ArgoCD: [Quản lý user và phân quyền trên ArgoCD](/kubernetes-practice/quan-ly-user-va-phan-quyen-tren-argocd).

**First AI Journey for DevOps:**

1. [The AIOps Book](https://leanpub.com/the-aiops-book)

## Bài viết liên quan

[Backup và Restore cho ArgoCD](/kubernetes/tips/backup-va-restore-cho-argocd)![Backup và Restore cho ArgoCD](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/c354430a-adca-48f8-aed5-e77a4e5822c0/kubernetes-tips-backup-restore-argocd.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/banana_gray.svg)

Backup và Restore cho ArgoCD

[Tạo và phân quyền người dùng trên Kubernetes](/kubernetes/tips/tao-va-phan-quyen-nguoi-dung-tren-kubernetes)![Tạo và phân quyền người dùng trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/ea5c2972-9a1d-4319-9588-3fbf37e59165/kubernetes-tips-rbac.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/child_gray.svg)

Tạo và phân quyền người dùng trên Kubernetes

[Kubernetes Tips - Giảm thời gian DNS Resolution của 10000 Pod trên EKS](/kubernetes/tips/giam-thoi-gian-dns-resolution-cua-10000-pod-tren-eks)![Kubernetes Tips - Giảm thời gian ](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/5946a8d2-6823-4ee0-a0e6-bea7672e2979/kubernetes-tips.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/transfers_gray.svg)

Kubernetes Tips - Giảm thời gian DNS Resolution của 10000 Pod trên EKS

## Highlighted / Emphasized Text

- Các bạn lưu ý chạy kubectl với namespace chính xác.
- First AI Journey for DevOps:

## Images

- ![Tạo và phân quyền người dùng trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/819ffec7-d947-4a94-92ad-c068b1e23e47/kubernetes-tips-rbac.drawio/w=1920,quality=90,fit=scale-down) — alt: "Tạo và phân quyền người dùng trên Kubernetes"
- ![Tạo và phân quyền người dùng trên Kubernetes](https://app.notion.com/icons/child_gray.svg) — alt: "Tạo và phân quyền người dùng trên Kubernetes"
- ![](https://lh3.googleusercontent.com/a-/AOh14GgQbiQWTwvWr9UNj32QaIqsed4xULOmoWj58imk=s100) — alt: ""
- ![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/ea5c2972-9a1d-4319-9588-3fbf37e59165/kubernetes-tips-rbac.drawio/w=1920,quality=90,fit=scale-down) — alt: "image"
- ![Backup và Restore cho ArgoCD](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/c354430a-adca-48f8-aed5-e77a4e5822c0/kubernetes-tips-backup-restore-argocd.drawio/w=1920,quality=90,fit=scale-down) — alt: "Backup và Restore cho ArgoCD"
- ![](https://app.notion.com/icons/banana_gray.svg) — alt: ""
- ![Tạo và phân quyền người dùng trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/ea5c2972-9a1d-4319-9588-3fbf37e59165/kubernetes-tips-rbac.drawio/w=1920,quality=90,fit=scale-down) — alt: "Tạo và phân quyền người dùng trên Kubernetes"
- ![](https://app.notion.com/icons/child_gray.svg) — alt: ""
- ![Kubernetes Tips - Giảm thời gian](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/5946a8d2-6823-4ee0-a0e6-bea7672e2979/kubernetes-tips.drawio/w=1920,quality=90,fit=scale-down) — alt: "Kubernetes Tips - Giảm thời gian"
- ![](https://app.notion.com/icons/transfers_gray.svg) — alt: ""

## Code Blocks

```yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: pod-logs
  namespace: dev
secrets:
  - name: pod-logs

---
apiVersion: v1
kind: Secret
metadata:
  name: pod-logs
  namespace: dev
  annotations:
    kubernetes.io/service-account.name: pod-logs
type: kubernetes.io/service-account-token
```

```none
kubectl apply -f pod-logs-sa.yaml
```

```yaml
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-logs
  namespace: dev
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log"]
  verbs: ["get", "list"]
```

```none
kubectl apply -f pod-logs-role.yaml
```

```none
kubectl -n dev create rolebinding pod-logs --serviceaccount=dev:pod-logs --role=pod-logs
```

```none
kubectl -n dev describe secrets "$(kubectl -n dev describe serviceaccount pod-logs | grep -i Tokens | awk '{print $2}')" | grep token: | awk '{print $2}'
```

```none
TOKEN=$(kubectl -n dev describe secrets "$(kubectl -n dev describe serviceaccount pod-logs | grep -i Tokens | awk '{print $2}')" | grep token: | awk '{print $2}')
```

```none
kubectl config set-credentials devopsvn --token=$TOKEN
```

```none
User "devopsvn" set.
```

```none
kubectl config set-context $(kubectl config current-context) --user=devopsvn
```

```none
$ kubectl auth can-i get pods --namespace=dev
yes

$ kubectl auth can-i get pods --namespace=pro
no
```

```yaml
apiVersion: v1
clusters:
- cluster:
    certificate-authority-data: ...
    server: ...
  name: devopsvn-cluster
contexts:
- context:
    cluster: devopsvn-cluster
    user: devopsvn
  name: devopsvn-cluster
kind: Config
preferences: {}
users:
- name: devopsvn
  user:
    token: ...
```

## Tables

_(none)_

## Internal Links

- [Quản lý user và phân quyền trên ArgoCD](https://devopsvn.tech/kubernetes-practice/quan-ly-user-va-phan-quyen-tren-argocd)
- [Backup và Restore cho ArgoCD](https://devopsvn.tech/kubernetes/tips/backup-va-restore-cho-argocd)
- [Tạo và phân quyền người dùng trên Kubernetes](https://devopsvn.tech/kubernetes/tips/tao-va-phan-quyen-nguoi-dung-tren-kubernetes)
- [Kubernetes Tips - Giảm thời gian DNS Resolution của 10000 Pod trên EKS](https://devopsvn.tech/kubernetes/tips/giam-thoi-gian-dns-resolution-cua-10000-pod-tren-eks)

## External Links

- [The AIOps Book](https://leanpub.com/the-aiops-book)
