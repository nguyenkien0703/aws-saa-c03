# Sự cố file Backup Database 0 byte do thiếu biến môi trường trong Cronjob

## Metadata

- **URL:** https://devops.vn/posts/su-co-file-backup-database-0-byte/
- **Title:** Sự cố file Backup Database 0 byte do thiếu biến môi trường trong Cronjob
- **OG Title:** Sự cố file Backup Database 0 byte do thiếu biến môi trường trong Cronjob
- **H1:** Sự cố file Backup Database 0 byte do thiếu biến môi trường trong Cronjob
- **Meta Description:** Mình dựng một con Linux Server chạy Database (MySQL) và nhận yêu cầu setup quy trình backup tự động vào 3 giờ sáng hàng ngày...
- **Content scope:** dv-post-content

## Headings

  - (H2) Cú lừa lúc 3 giờ sáng
  - (H2) Debug và tìm ra nguyên nhân
    - (H3) Tại sao chạy tay thì được, Cron chạy thì lỗi?
  - (H2) Giải pháp tối ưu
    - (H3) Bước 1: Tạo file chứa mật khẩu credentials
    - (H3) Bước 2: Viết lại Script hoàn chỉnh
    - (H3) Bước 3: Phân quyền (Bước quan trọng hay bị quên)
    - (H3) Bước 4: Cập nhật lại Crontab
  - (H2) Lời kết

## Content

Mình dựng một con Linux Server chạy Database (MySQL) và nhận yêu cầu setup quy trình backup tự động vào 3 giờ sáng hàng ngày.

Mình viết nhanh một Shell script `backup_db.sh`. Vì muốn bảo mật, mình không hardcode mật khẩu vào script mà gọi biến `$DB_USER` và `$DB_PASS` từ môi trường (được khai báo sẵn trong `.bashrc`).

![Sự cố file Backup Database 0 byte](https://devops.vn/uploads/images/2025/12/24fdf3d5-60ee-45de-bb7b-9cc4690d973b.webp)

Code trông gọn gàng thế này:

```bash
#!/bin/bash
# Biến DB_USER và DB_PASS lấy từ môi trường
mysqldump -u $DB_USER -p$DB_PASS my_database > /backup/db_$(date +%F).sql
```

Quy trình test diễn ra suôn sẻ: SSH vào server, chạy thử `./backup_db.sh`, kết quả trả về file SQL đầy đủ dữ liệu. Tự tin script đã ngon, mình gán nó vào Crontab: 

`0 3 * * * /home/user/scripts/backup_db.sh`

## Cú lừa lúc 3 giờ sáng

Sáng hôm sau kiểm tra:

- Trong thư mục `/backup`, file có được tạo ra nhưng **dung lượng là 0 bytes**.
- Kiểm tra syslog, Cron daemon báo lệnh đã thực thi đúng giờ, không có báo lỗi.

Lúc này, phản xạ đầu tiên thường dẫn chúng ta đi sai hướng:

- Nghi do user chạy Cron không có quyền ghi vào thư mục backup? Mình thử `chmod 777` nhưng file vẫn rỗng.
- Nghi đầy ổ cứng? Check `df -h` thấy vẫn còn trống cả trăm GB.
- Nghi script logic sai? Chạy lại bằng tay thì vẫn thành công.

## Debug và tìm ra nguyên nhân

Để biết chính xác chuyện gì đang xảy ra, mình sửa lại Crontab để bắn toàn bộ log ra file thay vì để nó chạy “mù”:

```bash
0 3 * * * /home/user/scripts/backup_db.sh > /tmp/backup_debug.log 2>&1
```

Đọc file log, nguyên nhân lộ diện ngay lập tức. Lỗi trả về là `mysqldump: command not found` hoặc `Access denied for user...`

### Tại sao chạy tay thì được, Cron chạy thì lỗi?

Vấn đề nằm ở sự khác biệt môi trường:

1. **Thiếu biến PATH:** Khi bạn SSH vào, bạn dùng **Shell tương tác**. Nó load đầy đủ file cấu hình nên biết lệnh [mysqldump](https://dev.mysql.com/doc/refman/8.4/en/mysqldump.html) nằm ở đâu. Ngược lại, Cron chạy **shell không tương tác** với biến `PATH` cực kỳ tối giản (thường chỉ có `/usr/bin:/bin`). Nếu `mysqldump` nằm ở `/usr/local/bin`, Cron sẽ không tìm thấy.
2. **Mất biến môi trường:** Cron không hề load file `.bashrc` hay `.profile`. Do đó, các biến `$DB_USER` và `$DB_PASS` hoàn toàn trống rỗng. Câu lệnh thực tế Cron chạy là `mysqldump -u -p ...` dẫn đến lỗi xác thực.

**Tại sao file lại 0 bytes?** 

Đây là cơ chế của shell: Khi gặp dấu điều hướng `>`, hệ thống sẽ tạo file rỗng trước để chuẩn bị nhận dữ liệu. Sau đó lệnh `mysqldump` mới chạy. Do lệnh lỗi và thoát ngay lập tức, không có byte dữ liệu nào được ghi vào, để lại một xác file rỗng tuếch.

## Giải pháp tối ưu

Để khắc phục triệt để, chúng ta cần làm theo trình tự 4 bước sau, đảm bảo 3 yếu tố: **Đường dẫn tuyệt đối**, **Bảo mật** và **Cơ chế tự ngắt lỗi**.

### Bước 1: Tạo file chứa mật khẩu credentials

Để bảo mật, tránh lộ mật khẩu khi ai đó soi process trên server. Tạo file `nano /home/user/.db-creds.cnf`:

```ini
[client]
user=backup_user
password=secret_password
host=localhost
```

### Bước 2: Viết lại Script hoàn chỉnh

Tạo file `nano /home/user/scripts/backup_db.sh`. Script này áp dụng đủ các nguyên tắc an toàn:

```bash
#!/bin/bash

# Dừng script ngay nếu có lỗi
set -e
# Quan trọng: Bắt lỗi ngay cả khi nó xảy ra trong pipeline
# Nếu mysqldump lỗi thì gzip cũng phải dừng theo
set -o pipefail

# Cấu hình đường dẫn
BACKUP_DIR="/backup"
DATE=$(date +%F)
CRED_FILE="/home/user/.db-creds.cnf"

# KHÔNG dùng lệnh tắt, hãy dùng đường dẫn tuyệt đối (check bằng lệnh `which mysqldump`)
MYSQLDUMP="/usr/bin/mysqldump"
GZIP="/bin/gzip"

# Đảm bảo thư mục tồn tại
mkdir -p "$BACKUP_DIR"

echo "Bắt đầu backup ngày $DATE..."

# Chạy backup kết hợp nén
# --defaults-extra-file giúp nạp user/pass an toàn
$MYSQLDUMP --defaults-extra-file=$CRED_FILE my_database | $GZIP > "$BACKUP_DIR/db_$DATE.sql.gz"

# Chính sách lưu trữ: Xóa file cũ hơn 30 ngày để tránh đầy ổ cứng
find "$BACKUP_DIR" -name "db_*.sql.gz" -mtime +30 -delete

echo "Backup thành công: $BACKUP_DIR/db_$DATE.sql.gz"
```

### Bước 3: Phân quyền (Bước quan trọng hay bị quên)

[Linux](https://devops.vn/tag/linux) mặc định không cho phép chạy các file text mới tạo. Bạn cần cấp quyền thực thi cho script và khóa quyền đọc file mật khẩu.

```bash
# Chỉ cho chủ sở hữu đọc file mật khẩu (Bảo mật)
chmod 600 /home/user/.db-creds.cnf

# Cấp quyền thực thi cho script backup (Bắt buộc để Cron chạy được)
chmod +x /home/user/scripts/backup_db.sh
```

### Bước 4: Cập nhật lại Crontab

Sửa lại Crontab (`crontab -e`) để lưu lại lịch sử chạy, giúp dễ dàng kiểm tra sau này.

```bash
# Chạy vào 03:00 sáng mỗi ngày, ghi nối log vào file
0 3 * * * /home/user/scripts/backup_db.sh >> /var/log/db_backup.log 2>&1
```

## Lời kết

Việc chuyển đổi từ thao tác thủ công sang tự động hóa không đơn giản chỉ là copy-paste lệnh vào Crontab. Sự cố file rỗng này là lời nhắc nhở rằng Cronjob là một môi trường rất “khắt khe” và khác biệt hoàn toàn so với những gì chúng ta thao tác tay hàng ngày.

Để có những giấc ngủ ngon và không phải giật mình lúc nửa đêm, hãy đảm bảo mọi script tự động của bạn luôn tự chủ về đường dẫn, bảo mật về thông tin và không bao giờ im lặng khi gặp lỗi.

## Highlight Text

- dung lượng là 0 bytes
- Thiếu biến PATH:
- Shell tương tác
- shell không tương tác
- Mất biến môi trường:
- Tại sao file lại 0 bytes?
- Đường dẫn tuyệt đối
- Bảo mật
- Cơ chế tự ngắt lỗi

## Images

- ![Sự cố file Backup Database 0 byte](https://devops.vn/uploads/images/2025/12/24fdf3d5-60ee-45de-bb7b-9cc4690d973b.webp)  — alt: `Sự cố file Backup Database 0 byte`

## Code Blocks

```
#!/bin/bash
# Biến DB_USER và DB_PASS lấy từ môi trường
mysqldump -u $DB_USER -p$DB_PASS my_database > /backup/db_$(date +%F).sql
```

```
0 3 * * * /home/user/scripts/backup_db.sh > /tmp/backup_debug.log 2>&1
```

```
[client]
user=backup_user
password=secret_password
host=localhost
```

```
#!/bin/bash

# Dừng script ngay nếu có lỗi
set -e
# Quan trọng: Bắt lỗi ngay cả khi nó xảy ra trong pipeline
# Nếu mysqldump lỗi thì gzip cũng phải dừng theo
set -o pipefail

# Cấu hình đường dẫn
BACKUP_DIR="/backup"
DATE=$(date +%F)
CRED_FILE="/home/user/.db-creds.cnf"

# KHÔNG dùng lệnh tắt, hãy dùng đường dẫn tuyệt đối (check bằng lệnh `which mysqldump`)
MYSQLDUMP="/usr/bin/mysqldump"
GZIP="/bin/gzip"

# Đảm bảo thư mục tồn tại
mkdir -p "$BACKUP_DIR"

echo "Bắt đầu backup ngày $DATE..."

# Chạy backup kết hợp nén
# --defaults-extra-file giúp nạp user/pass an toàn
$MYSQLDUMP --defaults-extra-file=$CRED_FILE my_database | $GZIP > "$BACKUP_DIR/db_$DATE.sql.gz"

# Chính sách lưu trữ: Xóa file cũ hơn 30 ngày để tránh đầy ổ cứng
find "$BACKUP_DIR" -name "db_*.sql.gz" -mtime +30 -delete

echo "Backup thành công: $BACKUP_DIR/db_$DATE.sql.gz"
```

```
# Chỉ cho chủ sở hữu đọc file mật khẩu (Bảo mật)
chmod 600 /home/user/.db-creds.cnf

# Cấp quyền thực thi cho script backup (Bắt buộc để Cron chạy được)
chmod +x /home/user/scripts/backup_db.sh
```

```
# Chạy vào 03:00 sáng mỗi ngày, ghi nối log vào file
0 3 * * * /home/user/scripts/backup_db.sh >> /var/log/db_backup.log 2>&1
```

## Tables

_(none)_

## Internal Links

- [https://devops.vn/expert-insights/ung-dung-ai-trong-devops-liam-nguyen/](https://devops.vn/expert-insights/ung-dung-ai-trong-devops-liam-nguyen/)
- [https://devops.vn](https://devops.vn)
- [Hỗ trợ?](https://devops.vn/support/)
- [Đăng nhập](https://devops.vn/login)
- [Đăng ký](https://devops.vn/signup)
- [Series](https://devops.vn/tag/devops-series/)
- [Mindset](https://devops.vn/tag/mindset/)
- [Linux](https://devops.vn/tag/linux/)
- [Bash](https://devops.vn/tag/bash/)
- [Docker](https://devops.vn/tag/docker/)
- [Kubernetes](https://devops.vn/tag/kubernetes/)
- [IaC](https://devops.vn/tag/infrastructure-as-code/)
- [Security](https://devops.vn/tag/security/)
- [Networking](https://devops.vn/tag/networking/)
- [Cloud](https://devops.vn/tag/cloud/)
- [Monitoring](https://devops.vn/tag/monitoring/)
- [Certificate](https://devops.vn/tag/certificate/)
- [AI](https://devops.vn/tag/ai/)
- [Tài nguyên](https://devops.vn/category/posts/)
- [Newsletter](https://devops.vn/newsletter/)
- [Giới thiệu](https://devops.vn/about/)
- [Blog](https://devops.vn/knowledge/)
- [Flashcards](https://devops.vn/flashcards/)
- [Cheat sheets](https://devops.vn/cheat-sheets/)
- [Sự kiện](https://devops.vn/events/)
- [Tin tức](https://devops.vn/news/)
- [Dành cho doanh nghiệp](https://devops.vn/vi/case-studies/)
- [Trí Hồ](https://devops.vn/author/trihodevops/)
- [13/12/2025](https://devops.vn/2025/12/13/)
- [Linux](https://devops.vn/tag/linux)
- [Backup](https://devops.vn/tag/backup/)
- [Cronjob](https://devops.vn/tag/cronjob/)
- [Database](https://devops.vn/tag/database/)
- [Shell](https://devops.vn/tag/shell/)
- [https://devops.vn/event/devops-vietnam-x-linux-foundation/](https://devops.vn/event/devops-vietnam-x-linux-foundation/)
- [https://devops.vn/event/state-of-devops-vietnam-2026/](https://devops.vn/event/state-of-devops-vietnam-2026/)
- [https://devops.vn/new/gap-go-trinh-the-long-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-java/](https://devops.vn/new/gap-go-trinh-the-long-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-java/)
- [https://devops.vn/new/gap-go-tran-quoc-toan-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-reactjs/](https://devops.vn/new/gap-go-tran-quoc-toan-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-reactjs/)
- [https://devops.vn/new/gap-go-huynh-quang-tien-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-java/](https://devops.vn/new/gap-go-huynh-quang-tien-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-java/)
- [https://devops.vn/new/gap-go-ngo-tan-tai-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-python/](https://devops.vn/new/gap-go-ngo-tan-tai-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-python/)
- [https://devops.vn/new/gap-go-nguyen-the-thanh-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-python/](https://devops.vn/new/gap-go-nguyen-the-thanh-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-python/)
- [https://devops.vn/new/gap-go-nguyen-huu-phuong-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-reactjs/](https://devops.vn/new/gap-go-nguyen-huu-phuong-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-reactjs/)
- [https://devops.vn/new/gap-go-nguyen-phuc-bao-lam-vo-dich-dockerfile-contest-2025-docker-image-nho-nhat/](https://devops.vn/new/gap-go-nguyen-phuc-bao-lam-vo-dich-dockerfile-contest-2025-docker-image-nho-nhat/)
- [DevOps VietNam](https://devops.vn/)
- [https://devops.vn/feed](https://devops.vn/feed)
- [Hỗ trợ](https://devops.vn/support)
- [Ebooks](https://devops.vn/ebooks)
- [Điều khoản sử dụng](https://devops.vn/terms/)
- [Chính sách bảo mật](https://devops.vn/privacy/)
- [https://devops.vn/posts/tai-sao-ufw-vo-tac-dung-voi-docker/](https://devops.vn/posts/tai-sao-ufw-vo-tac-dung-voi-docker/)
- [https://devops.vn/posts/memory-con-trong-nhung-kubernetes-container-van-oomkilled/](https://devops.vn/posts/memory-con-trong-nhung-kubernetes-container-van-oomkilled/)

## External Links

- [mysqldump](https://dev.mysql.com/doc/refman/8.4/en/mysqldump.html)
- [https://www.facebook.com/devopsvietnam.fanpage](https://www.facebook.com/devopsvietnam.fanpage)
- [https://www.tiktok.com/@devops.vn](https://www.tiktok.com/@devops.vn)
- [https://www.instagram.com/devops.vn/](https://www.instagram.com/devops.vn/)
- [https://www.linkedin.com/company/devopsvietnam](https://www.linkedin.com/company/devopsvietnam)
- [Email](mailto:support@devops.vn)
- [https://www.dmca.com/Protection/Status.aspx?ID=dd363dc3-580e-4a61-8f48-7b41fb97cf39](https://www.dmca.com/Protection/Status.aspx?ID=dd363dc3-580e-4a61-8f48-7b41fb97cf39)
- [https://transparencyreport.google.com/safe-browsing/search?url=devops.vn](https://transparencyreport.google.com/safe-browsing/search?url=devops.vn)
