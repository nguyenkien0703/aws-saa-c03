# Triển khai Elasticsearch lên trên Kubernetes Cloud

## Metadata

- **URL:** https://devopsvn.tech/kubernetes-practice/trien-khai-elasticsearch-len-tren-kubernetes-cloud
- **Title:** Triển khai Elasticsearch lên trên Kubernetes Cloud

## Headings

- H1: Triển khai Elasticsearch lên trên Kubernetes Cloud
- H2: Elastic Cloud on Kubernetes
- H3: Cài đặt ECK
- H3: Triển khai Elasticsearch với ECK
- H3: Truy cập Elasticsearch
- H3: Chỉnh sửa Persistent Volume
- H3: Public Elasticsearch
- H2: Sao lưu và phôi phục
- H2: Kết luận
- H2: Bài viết liên quan

## Content

![Triển khai Elasticsearch lên trên Kubernetes Cloud](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/7eecda0a-eab5-423a-807d-b78bc7d2a28b/eck/w=1920,quality=90,fit=scale-down)

![Triển khai Elasticsearch lên trên Kubernetes Cloud](https://app.notion.com/icons/connecting-flight_gray.svg)

# Triển khai Elasticsearch lên trên Kubernetes Cloud

Ngày đăng

Jan 15, 2023 1:35 PM

Tác giả

![](https://lh3.googleusercontent.com/a-/AOh14GgQbiQWTwvWr9UNj32QaIqsed4xULOmoWj58imk=s100)Quân Huỳnh

**Giới thiệu**

Ở bài này chúng ta sẽ tìm hiểu về cách triển khai Elasticsearch Cluster lên trên môi trường Cloud, cùng với đó là cách sao lưu và khôi phục dữ liệu của Elasticsearch.

**Mục lục**

- [Elastic Cloud on Kubernetes](#block-2acb050130e64e9fb570ab9025385412)
- [Cài đặt ECK](#block-10cff18cf9f54d3399abf9c33e3b3215)
- [Triển khai Elasticsearch với ECK](#block-4598e8936f5846ce89c4e62ae0237fd5)
- [Truy cập Elasticsearch](#block-e48fa087ce424e428120479c611b8afe)
- [Chỉnh sửa Persistent Volume](#block-76191057646e4e46b6e5ef255518f884)
- [Public Elasticsearch](#block-b72ab341b72d43dd9fa0b685420f9562)
- [Sao lưu và phôi phục](#block-46364720e4054de69c603ed19951fbc1)
- [Kết luận](#block-446c6a00bd3545e9a48e1f137ff4b595)
- [Bài viết liên quan](#block-dcab68d8883643b5b49274eaf864e523)

![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/a90bb885-43fe-43ab-b7c2-858ff0cd2eb8/eck/w=1920,quality=90,fit=scale-down)

Để triển khai Elasticsearch thì có rất nhiều cách (bài này bàn về triển khai Elasticsearch trên AWS):

- Xài AWS Opensearch (Elasticsearch Service): với Opensearch thì AWS sẽ quản lý việc mở rộng và HA cho ta, ưu điểm là dễ quản lý tuy nhiên tiền khá cao (hơn $400 tháng)
- Triển khai Elasticsearch lên trên Kubernetes: giá tiền thì chắc chắn sẽ ít hơn so với AWS Opensearch, ưu điểm là dễ mở rộng và quản lý, tuy nhiên yêu cầu là cần phải biết Kubernetes
- Triển khai Elasticsearch lên thẳng EC2: cách này thì sẽ không có HA sẵn và yêu cần ta phải tự cấu hình. Bitnami nó có một ELK AMI sẵn [ở đây](https://aws.amazon.com/marketplace/pp/prodview-tlbc33skxwrm6), ta chỉ cần tạo EC2 từ AMI này là được. Cách này thì giá tiền là ít nhất, chỉ cần con EC2 `t3a.medium` với 30GB. Nhưng về vấn đề HA, mở rộng, sao lưu, khôi phục dữ liệu thì ta phải tự làm

Tùy thuộc vào điều kiện thì ta sẽ chọn cách phù hợp, bài này sẽ hướng dẫn cách triển khai Elasticsearch lên trên Kubernetes.

## Elastic Cloud on Kubernetes

Bình thường để triển khai Elasticsearch thì ta sẽ viết tệp tin cấu hình StatefulSet cho Elasticsearch, hoặc dùng Helm Chart để triển khai Elasticsearch lên trên K8S. Tuy việc này không có gì sai nhưng để quản lý việc mở rộng và cấu hình bảo mật cho cụm Elasticsearch thì không đơn giản. Nên Elasticsearch Official có cung cấp cho ta sẵn một [Kubernetes Operator](https://viblo.asia/p/kubernetes-series-bai-19-adding-custom-resource-to-kubernetes-eW65GB8alDO) dùng cho Elasticsearch, tên là *Elastic Cloud on Kubernetes* (ECK). ECK giúp ta triển khai Elasticsearch lên trên K8S một cách rất dễ dàng.

Để sử dụng ECK đầu tiên ta phải cài CRDs và RBAC Rules của nó.

### Cài đặt ECK

Cài CRDs và RBAC:

Copy

```
kubectl create -f https://download.elastic.co/downloads/eck/1.9.1/crds.yaml
```

Cài Operator Controller cho ECK:

Copy

```
kubectl apply -f https://download.elastic.co/downloads/eck/1.9.1/operator.yaml
```

Kiểm tra Operator đã hoạt động chưa:

Copy

```
$ kubectl get sts elastic-operator

NAME               READY   AGE
elastic-operator   1/1     99s
```

Nếu cột READY là 1/1 thì ECK đã cài thành công.

### Triển khai Elasticsearch với ECK

Để triển khai Elasticsearch Cluster với ECK, ta sẽ dùng `apiVersion` là `elasticsearch.k8s.elastic.co/v1` với `kind` là `Elasticsearch`. Tạo một tệp tin tên là `elasticsearch.yaml`:

Copy

```
apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: elasticsearch
spec:
  version: 7.16.3
  nodeSets:
  - name: default
    count: 1
```

Copy

```
kubectl apply -f elasticsearch.yaml
```

Thuộc tính `version` để ta chọn `version` của Elasticsearch mà ta muốn dùng, thuộc tính `nodeSets` dùng để định nghĩa cụm Node cho Elasticsearch, ở trên ta chỉ định cụm Node có tên là `default` và số lượng Node là 1.

Kiểm tra Elasticsearch đã chạy chưa.

Copy

```
$ kubectl get elasticsearch

NAME            HEALTH   NODES   VERSION   PHASE   AGE
elasticsearch   yellow   1       7.16.3    Ready   4m53s
```

Nếu cột `PHASE` là `Ready` thì Elasticsearch đã chạy.

> Về bản chất khi ta tạo một Elasticsearch bằng ECK thì ECK sẽ tự động tạo StatefulSet ở bên dưới, kiểm tra thử.

Copy

```
$ kubectl get sts

NAME                       READY   AGE
elasticsearch-es-default   1/1     4m53s
```

Như bạn thấy sử dụng ECK giúp ta triển khai một cụm Elasticsearch cực kì dễ dàng, tuy nhiên nhược điểm là nó chỉ xài dễ dàng trên Cloud thôi 😂.

### Truy cập Elasticsearch

Để truy cập cụm Elasticsearch, ta có thể dùng `port-forward` như sau.

Copy

```
kubectl port-forward service/elasticsearch-es-http 9200
```

Sử dụng curl để gửi yêu cầu tới Elasticsearch.

Copy

Ta sẽ thấy Elasticsearch báo lỗi `401`. Vì khi ta tạo Elasticsearch dùng ECK thì mặc định nó sẽ bật chế độ **Security** lên, nên để truy cập tới nó ta cần phải có mật khẩu. Ta lấy mật khẩu của cụm Elasticsearch bằng câu lệnh sau.

Copy

```
kubectl get secret elasticsearch-es-elastic-user -o jsonpath={.data.elastic} | base64 --decode ; echo
```

Kết quả của câu lệnh trên sẽ in ra mật khẩu cho ta, gọi lại Elasticsearch với mật khẩu.

Copy

```
PASSWORD=$(kubectl get secret elasticsearch-es-elastic-user -n persistent -o jsonpath={.data.elastic} | base64 --decode)
curl -u "elastic:$PASSWORD" https://localhost:9200
```

Copy

```
{
  "name" : "elasticsearch-es-default-0",
  "cluster_name" : "default",
  "cluster_uuid" : "XqWg0xIiRmmEBg4NMhnYPg",
  "version" : {...},
  "tagline" : "You Know, for Search"
}
```

Vậy là ta đã triển khai Elasticsearch lên trên K8S thành công.

### Chỉnh sửa Persistent Volume

Mặc định ECK sẽ tạo ra PVC với kích thước là 1GB cho mỗi Pod.

Copy

```
$ kubectl get pvc

NAME                                            STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
elasticsearch-data-elasticsearch-es-default-0   Bound                                               1Gi        RWO            gp2            4m53s
```

Nếu ta muốn thay đổi kích thước PVC thì ta phải chỉ định thông qua trường `volumeClaimTemplates` Cập nhật lại PVC của tệp tin `elasticsearch.yaml`.

Copy

Copy

```
kubectl apply -f elasticsearch.yaml
```

Kiểm tra PVC.

Copy

```
$ kubectl get pvc

NAME                                            STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
elasticsearch-data-elasticsearch-es-default-0   Bound                                               10Gi       RWO            gp2            1m52s
```

Lúc này bạn sẽ thấy kích thước của PVC đã tăng lên 10GB.

### Public Elasticsearch

Để mở Elasticsearch cho bên ngoài có thể truy cập được thì ta sẽ dùng Ingress loại AWS Load Balancer. Để cài Ingress, các bạn xem hướng dẫn sau đây [EKS Ingress](https://aws.amazon.com/premiumsupport/knowledge-center/eks-alb-ingress-controller-fargate/).

Mặc định ECK tạo ra cho Elasticsearch Cluster bên dưới ba Service.

Copy

```
$ kubectl get svc

NAME                         TYPE        CLUSTER-IP     EXTERNAL-IP   PORT(S)    AGE
elasticsearch-es-default     ClusterIP   None           <none>        9200/TCP   7m52s
elasticsearch-es-http        ClusterIP   172.20.86.41   <none>        9200/TCP   7m52s
elasticsearch-es-transport   ClusterIP   None           <none>        9300/TCP   7m52s
```

Service mà nhận Request của người dùng là `elasticsearch-es-http`, ta sẽ tạo một Ingress chỉa tới nó. Tạo một tệp tin tên là `ingress-elasticsearch.yaml`.

Copy

- `alb.ingress.kubernetes.io/healthcheck-protocol: HTTPS` sẽ cấu hình phương thức AWS Load Balancer dùng để Health Check là HTTPS
- `alb.ingress.kubernetes.io/success-codes: '401'` vì khi ta dùng ECK để triển khai Elasticsearch, mặc định nó sẽ bật chế độ Security nên tất cả các đường dẫn đều cần Authenticate mới gọi được. Vì hiện tại ALB không cho cấu hình truyền `body` vào trong ALB Health Check, nên ta phải thay đổi mã HTTP thành 401
- `alb.ingress.kubernetes.io/backend-protocol: HTTPS` giao thức ALB gọi tới Pod sẽ là HTTPS
- `alb.ingress.kubernetes.io/certificate-arn` là ACM ARN, nhớ nhập chính xác nhé, ở trên chỉ là giá trị ví dụ
- `alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}, {"HTTPS":443}]'` ALB lắng nghe ở `port` 80 và 443

Truy cập Elasticsearch thông qua url `elastic.example.com`.

## Sao lưu và phôi phục

Với Elasticsearch để thực hiện sao lưu và phôi phục, ta sẽ sử dụng cơ chế *Snapshot and Restore*. Elasticsearch cho phép ta tạo Snapshot từ dữ liệu hiện tại, và có thể khôi phục dữ liệu từ Snapshot đó. Snapshot này ta có thể tự tạo bằng tay hoặc cấu hình cho Elasticsearch tự động tạo Snapshot trong một khoảng thời gian nhất định. Ta có thể lưu Snapshot ở Server hoặc ở trên Cloud đều được.

**Sao lưu**

Vì ta triển khai ECK ở trên AWS nên ta sẽ lưu Snapshot ở trên S3. Để thực hiện tạo Snapshot và lưu nó trên S3 ta cần làm các bước sau đây:

- Cài Elasticsearch Plugin cho S3
- Thêm Credentials vào Elasticsearch để nó quyền lưu dữ liệu vào S3
- Đăng ký Snapshot Repository bằng Elasticsearch API
- Tạo Snapshot bằng tay hoặc cấu hình cho Elasticsearch tự động tạo Snapshot

**Cài S3 Plugin**

Để cài Plugin bằng ECK khá đơn giản, ta chỉ cần thêm vào tệp tin cấu hình Elasticsearch một `initContainers` và cho nó thực hiện câu lệnh cài plugin, cập nhật lại tệp tin `elasticsearch.yaml`:

Copy

Đây là đoạn dùng để cài Plugin.

Copy

```
initContainers:
- name: install-plugins
  command:
  - sh
  - -c
  - |
    bin/elasticsearch-plugin install --batch repository-s3
```

**Thêm S3 Credentials**

Để thêm Credentials vào Elasticsearch ta tạo một IAM User có quyền trên S3, sau đó lấy `access_key` và `secret_key` của IAM User đó và thêm vào Elasticsearch, các bước như sau:

- Truy cập [IAM Console](https://console.aws.amazon.com/iam)
- Bấm vào mục Users chọn Add Users
- Nhập User Name và ở mục **Select AWS Credential Type** chọn vào **Access Key - Programmatic Access**, bấm Next Permission
- Chọn **Attach Existing Policies Directly** kiếm `AmazonS3FullAccess`, bấm **Next Tags** và bấm **Next Review**, sau đó tạo IAM User, nhớ tải xuống hoặc sao chép lại `ACCESS_KEY` và `SECRET_KEY`.

Sau đó tạo hai tệp tin với với tên là `s3.client.default.access_key` và `s3.client.default.secret_key`, sao chéo `ACCESS_KEY` dán vào tệp tin `s3.client.default.access_key` và `SECRET_KEY` dán vào tệp tin `s3.client.default.secret_key`, chạy câu lệnh sau để tạo Secret và gán nó vào Elasticsearch.

Copy

```
kubectl create secret generic s3-credentials --from-file=s3.client.default.access_key --from-file=s3.client.default.secret_key
```

Cập nhật lại tệp tin `elasticsearch.yaml` thêm vào đoạn cấu hình sau:

Copy

```
...
spec:
  version: 7.16.3
  secureSettings:
  - secretName: s3-credentials
    entries:
    - key: s3.client.default.access_key
    - key: s3.client.default.secret_key
...
```

Copy

```
kubectl apply -f elasticsearch.yaml
```

Vậy là ta đã thêm Credentials thành công.

**Đăng ký Snapshot Repository**

Tiếp theo là ta sẽ tạo một Repository để chứa toàn bộ Snapshot. Kiểm tra toàn bộ Repository hiện tại.

Copy

```
$ curl -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/_all

{}
```

Tạo Repository.

Copy

```
$ curl -XPUT -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot -d '{
  "type": "s3",
  "settings": {
    "client": "default",
    "bucket": "<your-bucket>",
    "region": "<region>"
  }
}'

{
  "acknowledged": true
}
```

Lúc này khi liệt kệ Repository ta sẽ thấy Repository ta vừa tạo.

Copy

```
$ curl -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/_all

{
  "s3_snapshot" : {
    "type" : "s3",
    "settings" : {
      "bucket" : "<your-bucket>",
      "region" : "<region>",
      "client" : "default"
    }
  }
}
```

Tạo Snapshot bằng tay.

Copy

```
curl -XPUT -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot/<snapshot-name>
```

Với `snapshot-name` là tên Snapshot, liệt kê toàn bộ Snapshot.

Copy

```
curl -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot/_all
```

**Khôi phục**

Nếu ta có lỡ xóa đi dữ liệu hoặc máy chủ ta bị chết, để khôi phục dữ liệu từ Snapshot ta chỉ việc gọi API như sau:

Copy

```
curl -XPOST -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot/<snapshot-name>/_restore
```

Elasticsearch sẽ khôi phục dữ liệu lại từ Snapshot.

**Tự động Snapshot**

Để tự động tạo Snapshot ta cần sử dụng Kibana. Truy cập vào Kibana, di chuyển tới phần **Management** -> **Snapshot and Restore** -> **Policies**, sau đó bấm tạo Policies và cấu hình thời gian tạo Snapshot.

![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/c84f3249-0ba2-4543-adad-59efbd5fb7a6/Untitled/w=1920,quality=90,fit=scale-down)

Hoàn thành việc cấu hình Snapshot tự động.

## Kết luận

Vậy là ta đã tìm hiểu xong cách triển khai Elasticsearch lên trên Kubernetes bằng cách sử dụng ECK, sử dùng ECK giúp ta triển khai Elasticsearch rất dễ dàng.

Tác giả @Quân Huỳnh

**First AI Journey for DevOps:**

1. [The AIOps Book](https://leanpub.com/the-aiops-book)

## Bài viết liên quan

[A/B Testing Deployment](/kubernetes-practice/ab-testing-deployment)![A/B Testing Deployment](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/917df02a-1146-4b8d-a877-2b764a90fc40/Kubernetes_co_ban-2/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/branch_gray.svg)

A/B Testing Deployment

[Kubernetes Gateway API](/kubernetes-practice/gateway-api)![Kubernetes Gateway API](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/4927680b-d348-4b4d-92d2-280979958886/Untitled/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/alien-pixel_gray.svg)

Kubernetes Gateway API

[Kubernetes Logging với Logstash và FluentD](/kubernetes-practice/kubernetes-logging-voi-logstash-va-fluentd)![Kubernetes Logging với Logstash và FluentD](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/4f9be562-edb3-4fbe-a0f0-7ba09009e631/kubernetes-practice-logging-00.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/document_gray.svg)

Kubernetes Logging với Logstash và FluentD

[Triển khai Elasticsearch lên trên Kubernetes Cloud](/kubernetes-practice/trien-khai-elasticsearch-len-tren-kubernetes-cloud)![Triển khai Elasticsearch lên trên Kubernetes Cloud](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/a90bb885-43fe-43ab-b7c2-858ff0cd2eb8/eck/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/connecting-flight_gray.svg)

Triển khai Elasticsearch lên trên Kubernetes Cloud

[Cấu hình PostgreSQL Master Slave Replication trên Kubernetes](/kubernetes-practice/cau-hinh-postgresql-master-slave-replication-tren-kubernetes)![Cấu hình PostgreSQL Master Slave Replication trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/b0f6ad80-b73b-49f3-8bc5-66555183d8d5/kubernetes-practice-master-slave/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/cards_gray.svg)

Cấu hình PostgreSQL Master Slave Replication trên Kubernetes

[Triển khai hệ thống Microservices lên trên Kubernetes](/kubernetes-practice/trien-khai-he-thong-microservices-len-tren-kubernetes)![Triển khai hệ thống Microservices lên trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/2464b05a-c301-4fd9-b4b2-6fdcfb380a8a/kubernetes-practice-demo-micro/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/capricorn_gray.svg)

Triển khai hệ thống Microservices lên trên Kubernetes

[Kubernetes based Event Driven Autoscaler](/kubernetes-practice/kubernetes-based-event-driven-autoscaler)![Kubernetes based Event Driven Autoscaler](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/4aa52acf-fa76-49a6-a49c-7a2d1bd13ee6/kubernetes-practice-keda.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/computer-chip_gray.svg)

Kubernetes based Event Driven Autoscaler

[Debug with Ephemeral Container](/kubernetes-practice/debug-with-ephemeral-container)![Debug with Ephemeral Container](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/7b6a0a6c-a655-45fd-a62e-8f68da6fbe56/kubernetes-practice-Debug.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/computer-chip_gray.svg)

Debug with Ephemeral Container

[Blue Green Deployment with Argo Rollouts](/kubernetes-practice/blue-green-deployment-with-argo-rollouts)![Blue Green Deployment with Argo Rollouts](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/561a4397-f452-4091-8480-a6502cc33438/argorollout/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/bounce_gray.svg)

Blue Green Deployment with Argo Rollouts

[Manual Blue/Green Deployment](/kubernetes-practice/manual-bluegreen-deployment)![Manual Blue/Green Deployment](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/56ab6d09-899a-4bd7-a8da-77b63968cdd8/kubernetes-practice-blue-green.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/computer-chip_gray.svg)

Manual Blue/Green Deployment

[Quản lý user và phân quyền trên ArgoCD](/kubernetes-practice/quan-ly-user-va-phan-quyen-tren-argocd)![Quản lý user và phân quyền trên ArgoCD](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/2b98ba42-9fc0-4af4-9733-2611cb92c02a/kubernetes-practice-argo-user.drawio/w=1920,quality=90,fit=scale-down)

![](https://app.notion.com/icons/computer-chip_gray.svg)

Quản lý user và phân quyền trên ArgoCD

## Highlighted / Emphasized Text

- Giới thiệu
- Mục lục
- Security
- Sao lưu
- Cài S3 Plugin
- Thêm S3 Credentials
- Select AWS Credential Type
- Access Key - Programmatic Access
- Attach Existing Policies Directly
- Next Tags
- Next Review
- Đăng ký Snapshot Repository
- Khôi phục
- Tự động Snapshot
- Management
- Snapshot and Restore
- Policies
- First AI Journey for DevOps:

## Images

- ![Triển khai Elasticsearch lên trên Kubernetes Cloud](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/7eecda0a-eab5-423a-807d-b78bc7d2a28b/eck/w=1920,quality=90,fit=scale-down) — alt: "Triển khai Elasticsearch lên trên Kubernetes Cloud"
- ![Triển khai Elasticsearch lên trên Kubernetes Cloud](https://app.notion.com/icons/connecting-flight_gray.svg) — alt: "Triển khai Elasticsearch lên trên Kubernetes Cloud"
- ![](https://lh3.googleusercontent.com/a-/AOh14GgQbiQWTwvWr9UNj32QaIqsed4xULOmoWj58imk=s100) — alt: ""
- ![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/a90bb885-43fe-43ab-b7c2-858ff0cd2eb8/eck/w=1920,quality=90,fit=scale-down) — alt: "image"
- ![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/c84f3249-0ba2-4543-adad-59efbd5fb7a6/Untitled/w=1920,quality=90,fit=scale-down) — alt: "image"
- ![A/B Testing Deployment](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/917df02a-1146-4b8d-a877-2b764a90fc40/Kubernetes_co_ban-2/w=1920,quality=90,fit=scale-down) — alt: "A/B Testing Deployment"
- ![](https://app.notion.com/icons/branch_gray.svg) — alt: ""
- ![Kubernetes Gateway API](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/4927680b-d348-4b4d-92d2-280979958886/Untitled/w=1920,quality=90,fit=scale-down) — alt: "Kubernetes Gateway API"
- ![](https://app.notion.com/icons/alien-pixel_gray.svg) — alt: ""
- ![Kubernetes Logging với Logstash và FluentD](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/4f9be562-edb3-4fbe-a0f0-7ba09009e631/kubernetes-practice-logging-00.drawio/w=1920,quality=90,fit=scale-down) — alt: "Kubernetes Logging với Logstash và FluentD"
- ![](https://app.notion.com/icons/document_gray.svg) — alt: ""
- ![Triển khai Elasticsearch lên trên Kubernetes Cloud](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/a90bb885-43fe-43ab-b7c2-858ff0cd2eb8/eck/w=1920,quality=90,fit=scale-down) — alt: "Triển khai Elasticsearch lên trên Kubernetes Cloud"
- ![](https://app.notion.com/icons/connecting-flight_gray.svg) — alt: ""
- ![Cấu hình PostgreSQL Master Slave Replication trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/b0f6ad80-b73b-49f3-8bc5-66555183d8d5/kubernetes-practice-master-slave/w=1920,quality=90,fit=scale-down) — alt: "Cấu hình PostgreSQL Master Slave Replication trên Kubernetes"
- ![](https://app.notion.com/icons/cards_gray.svg) — alt: ""
- ![Triển khai hệ thống Microservices lên trên Kubernetes](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/2464b05a-c301-4fd9-b4b2-6fdcfb380a8a/kubernetes-practice-demo-micro/w=1920,quality=90,fit=scale-down) — alt: "Triển khai hệ thống Microservices lên trên Kubernetes"
- ![](https://app.notion.com/icons/capricorn_gray.svg) — alt: ""
- ![Kubernetes based Event Driven Autoscaler](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/4aa52acf-fa76-49a6-a49c-7a2d1bd13ee6/kubernetes-practice-keda.drawio/w=1920,quality=90,fit=scale-down) — alt: "Kubernetes based Event Driven Autoscaler"
- ![](https://app.notion.com/icons/computer-chip_gray.svg) — alt: ""
- ![Debug with Ephemeral Container](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/7b6a0a6c-a655-45fd-a62e-8f68da6fbe56/kubernetes-practice-Debug.drawio/w=1920,quality=90,fit=scale-down) — alt: "Debug with Ephemeral Container"
- ![](https://app.notion.com/icons/computer-chip_gray.svg) — alt: ""
- ![Blue Green Deployment with Argo Rollouts](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/561a4397-f452-4091-8480-a6502cc33438/argorollout/w=1920,quality=90,fit=scale-down) — alt: "Blue Green Deployment with Argo Rollouts"
- ![](https://app.notion.com/icons/bounce_gray.svg) — alt: ""
- ![Manual Blue/Green Deployment](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/56ab6d09-899a-4bd7-a8da-77b63968cdd8/kubernetes-practice-blue-green.drawio/w=1920,quality=90,fit=scale-down) — alt: "Manual Blue/Green Deployment"
- ![](https://app.notion.com/icons/computer-chip_gray.svg) — alt: ""
- ![Quản lý user và phân quyền trên ArgoCD](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/2b98ba42-9fc0-4af4-9733-2611cb92c02a/kubernetes-practice-argo-user.drawio/w=1920,quality=90,fit=scale-down) — alt: "Quản lý user và phân quyền trên ArgoCD"
- ![](https://app.notion.com/icons/computer-chip_gray.svg) — alt: ""

## Code Blocks

```bash
kubectl create -f https://download.elastic.co/downloads/eck/1.9.1/crds.yaml
```

```bash
kubectl apply -f https://download.elastic.co/downloads/eck/1.9.1/operator.yaml
```

```none
$ kubectl get sts elastic-operator

NAME               READY   AGE
elastic-operator   1/1     99s
```

```yaml
apiVersion: elasticsearch.k8s.elastic.co/v1
kind: Elasticsearch
metadata:
  name: elasticsearch
spec:
  version: 7.16.3
  nodeSets:
  - name: default
    count: 1
```

```none
kubectl apply -f elasticsearch.yaml
```

```none
$ kubectl get elasticsearch

NAME            HEALTH   NODES   VERSION   PHASE   AGE
elasticsearch   yellow   1       7.16.3    Ready   4m53s
```

```none
$ kubectl get sts

NAME                       READY   AGE
elasticsearch-es-default   1/1     4m53s
```

```none
kubectl port-forward service/elasticsearch-es-http 9200
```

```none
kubectl get secret elasticsearch-es-elastic-user -o jsonpath={.data.elastic} | base64 --decode ; echo
```

```none
PASSWORD=$(kubectl get secret elasticsearch-es-elastic-user -n persistent -o jsonpath={.data.elastic} | base64 --decode)
curl -u "elastic:$PASSWORD" https://localhost:9200
```

```json
{
  "name" : "elasticsearch-es-default-0",
  "cluster_name" : "default",
  "cluster_uuid" : "XqWg0xIiRmmEBg4NMhnYPg",
  "version" : {...},
  "tagline" : "You Know, for Search"
}
```

```none
$ kubectl get pvc

NAME                                            STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
elasticsearch-data-elasticsearch-es-default-0   Bound                                               1Gi        RWO            gp2            4m53s
```

```none
kubectl apply -f elasticsearch.yaml
```

```none
$ kubectl get pvc

NAME                                            STATUS   VOLUME                                     CAPACITY   ACCESS MODES   STORAGECLASS   AGE
elasticsearch-data-elasticsearch-es-default-0   Bound                                               10Gi       RWO            gp2            1m52s
```

```none
$ kubectl get svc

NAME                         TYPE        CLUSTER-IP     EXTERNAL-IP   PORT(S)    AGE
elasticsearch-es-default     ClusterIP   None           <none>        9200/TCP   7m52s
elasticsearch-es-http        ClusterIP   172.20.86.41   <none>        9200/TCP   7m52s
elasticsearch-es-transport   ClusterIP   None           <none>        9300/TCP   7m52s
```

```yaml
initContainers:
- name: install-plugins
  command:
  - sh
  - -c
  - |
    bin/elasticsearch-plugin install --batch repository-s3
```

```bash
kubectl create secret generic s3-credentials --from-file=s3.client.default.access_key --from-file=s3.client.default.secret_key
```

```yaml
...
spec:
  version: 7.16.3
  secureSettings:
  - secretName: s3-credentials
    entries:
    - key: s3.client.default.access_key
    - key: s3.client.default.secret_key
...
```

```none
kubectl apply -f elasticsearch.yaml
```

```none
$ curl -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/_all

{}
```

```none
$ curl -XPUT -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot -d '{
  "type": "s3",
  "settings": {
    "client": "default",
    "bucket": "<your-bucket>",
    "region": "<region>"
  }
}'

{
  "acknowledged": true
}
```

```none
$ curl -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/_all

{
  "s3_snapshot" : {
    "type" : "s3",
    "settings" : {
      "bucket" : "<your-bucket>",
      "region" : "<region>",
      "client" : "default"
    }
  }
}
```

```none
curl -XPUT -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot/<snapshot-name>
```

```none
curl -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot/_all
```

```none
curl -XPOST -u "elastic:$PASSWORD" https://localhost:9200/_snapshot/s3_snapshot/<snapshot-name>/_restore
```

```
Copy
```

```
Copy
```

```
Copy
```

```
Copy
```

## Tables

_(none)_

## Internal Links

- [A/B Testing Deployment](https://devopsvn.tech/kubernetes-practice/ab-testing-deployment)
- [Kubernetes Gateway API](https://devopsvn.tech/kubernetes-practice/gateway-api)
- [Kubernetes Logging với Logstash và FluentD](https://devopsvn.tech/kubernetes-practice/kubernetes-logging-voi-logstash-va-fluentd)
- [Triển khai Elasticsearch lên trên Kubernetes Cloud](https://devopsvn.tech/kubernetes-practice/trien-khai-elasticsearch-len-tren-kubernetes-cloud)
- [Cấu hình PostgreSQL Master Slave Replication trên Kubernetes](https://devopsvn.tech/kubernetes-practice/cau-hinh-postgresql-master-slave-replication-tren-kubernetes)
- [Triển khai hệ thống Microservices lên trên Kubernetes](https://devopsvn.tech/kubernetes-practice/trien-khai-he-thong-microservices-len-tren-kubernetes)
- [Kubernetes based Event Driven Autoscaler](https://devopsvn.tech/kubernetes-practice/kubernetes-based-event-driven-autoscaler)
- [Debug with Ephemeral Container](https://devopsvn.tech/kubernetes-practice/debug-with-ephemeral-container)
- [Blue Green Deployment with Argo Rollouts](https://devopsvn.tech/kubernetes-practice/blue-green-deployment-with-argo-rollouts)
- [Manual Blue/Green Deployment](https://devopsvn.tech/kubernetes-practice/manual-bluegreen-deployment)
- [Quản lý user và phân quyền trên ArgoCD](https://devopsvn.tech/kubernetes-practice/quan-ly-user-va-phan-quyen-tren-argocd)

## External Links

- [ở đây](https://aws.amazon.com/marketplace/pp/prodview-tlbc33skxwrm6)
- [Kubernetes Operator](https://viblo.asia/p/kubernetes-series-bai-19-adding-custom-resource-to-kubernetes-eW65GB8alDO)
- [EKS Ingress](https://aws.amazon.com/premiumsupport/knowledge-center/eks-alb-ingress-controller-fargate/)
- [IAM Console](https://console.aws.amazon.com/iam)
- [The AIOps Book](https://leanpub.com/the-aiops-book)
