# Hạn chế Downtime khi migration PostgreSQL với Logical Replication

## Metadata

- **URL:** https://devops.vn/posts/han-che-downtime-khi-migration-postgresql-voi-logical-replication/
- **Title:** Hạn chế Downtime khi migration PostgreSQL với Logical Replication
- **OG Title:** Hạn chế Downtime khi migration PostgreSQL với Logical Replication
- **H1:** Hạn chế Downtime khi migration PostgreSQL với Logical Replication
- **Meta Description:** Tôi cũng lab để upgrade Postgres cho hệ thống đang chạy **production**, tại mỗi lần đụng DB là dừng service khá lâu. Xong có nghiên cứu thấy logical
- **Content scope:** dv-post-content

## Headings

  - (H2) Mục tiêu
  - (H2) Kiến trúc mẫu để hình dung
  - (H2) Chuẩn bị kiểm tra
  - (H2) Cấu hình trên source
    - (H3) Tạo role và publication trên source
  - (H2) Chuẩn bị destination
  - (H2) Tạo subscription trên destination DB
    - (H3) Giải thích nhanh
  - (H2) Theo dõi sync ban đầu
  - (H2) Đồng bộ các thay đổi không phải bảng
  - (H2) Kế hoạch Cutover
    - (H3) Trình tự Cutover
  - (H2) Chi tiết các bước Cutover
    - (H3) Bước 1: Kích hoạt Maintenance Mode
    - (H3) Bước 2: Monitor Replication Lag
    - (H3) Bước 3: Thực thi Write-Lock trên Source DB
    - (H3) Bước 4: Finalize State và Data Validation
    - (H3) Bước 5: Switch Endpoint PgBouncer
    - (H3) Bước 6: Enable Writes và Disable Maintenance
  - (H2) Parallel Operation
  - (H2) Post-Cutover Verification
  - (H2) Cleanup
  - (H2) Monitoring và Alerting
  - (H2) Fallback/Rollback Plan
    - (H3) Phương án A (Application-level issue)
    - (H3) Phương án B (Critical destination DB issue)
  - (H2) Tối ưu Performance (Bulk Data Copy)
  - (H2) Kết luận

## Content

Bài toán phải upgrade hạ tầng thì chắc không lạ nữa, tôi cũng thích đọc tech blog dạo trước cũng có nhiều thắc mắc là mấy doanh nghiệp hàng đầu, hay doanh nghiệp lớn một chút chẳng hạn họ làm kiểu gì mà upgrade lên giảm thiểu tối đa downtime mà đặc biệt upgade hệ thống đang chạy trên production.

![748bc866-cd6c-4ded-9fbe-57855a85bd32](https://devops.vn/uploads/images/2025/11/748bc866-cd6c-4ded-9fbe-57855a85bd32.webp)

Cái việc tác động vào đêm chắc nhiều anh em đã biết rồi, bank hay các hệ thống nhiều nhiều người dùng, đòi SLA cao tại Việt Nam thường sẽ vậy (chưa làm với nước ngoài nên cũng chẳng biết).

Tôi cũng lab để upgrade Postgres cho hệ thống đang chạy **production**, tại mỗi lần đụng DB là dừng service khá lâu. Xong có nghiên cứu thấy logical replication hợp lý. Làm đúng quy trình thì app vẫn chạy, chỉ cần dừng các thao tác ghi dữ liệu (INSERT, UPDATE) trong một thời gian rất ngắn ngay tại thời điểm thực hiện ccutover sang database mới. Thấy cũng hiệu quả và vừa làm vừa note, mọi người có thể tham khảo nếu gặp nhé hay ai có cái hay hơn share cho cộng đồng cùng học hỏi là tươm.

## Mục tiêu

- Cutover trong 1 đến 5 phút với rủi ro thấp.
- Không mất dữ liệu.
- Đo được replication lag và xác thực data consistency trước khi chuyển.
- Rollback Plan rõ ràng nếu phát sinh lỗi.

## Kiến trúc mẫu để hình dung

- **Source:** Postgres 14 (do Patroni quản lý hoặc single tùy mọi người nhé), cổng 5432.
- **Destination:** Postgres 15 (phần này dev bên tôi yêu cầu anh em muốn upgrade hơn cũng tương tự vậy), cổng 5433, dữ liệu trống.
- **App:** Kết nối qua PgBouncer để hạn chế số connection và đổi endpoint cho gọn.
- **Monitoring:** `postgres_exporter`, `pg_stat_replication`, `pg_stat_subscription`, `wal_level=logical`.

## Chuẩn bị kiểm tra

- Kiểm tra disk và bandwidth giữa hai DB. WAL sẽ được stream iên tục.
- Bật `wal_level=logical` trên source.
- Trên source, tạo user replication riêng, chỉ cấp quyền cần thiết.
- Đảm bảo replica destination có collation và timezone tương thích.
- Tạo bộ test đọc ghi nhỏ ở ứng dụng để xác nhận sau cutover.

## Cấu hình trên source

`postgresql.conf`

```ini
wal_level = logical
max_wal_senders = 10
max_replication_slots = 10
```

`pg_hba.conf` cho phép kết nối từ DB destination

```ini
host replication repl_user 192.168.1.0/24 scram-sha-256
host all          repl_user 192.168.1.0/24 scram-sha-256
```

### **Tạo role và publication trên source**

```sql
-- trên PG14
CREATE ROLE repl_user WITH REPLICATION LOGIN PASSWORD 'StrongPass!';

-- nếu app schema là public hoặc custom, chọn đúng schema
-- khuyến nghị publication theo schema để kiểm soát
CREATE PUBLICATION app_pub FOR ALL TABLES;

-- hoặc
-- CREATE PUBLICATION app_pub FOR TABLE schema_a.*, schema_b.*;
```

## Chuẩn bị destination

Tạo database trống, extensions cần thiết, role ứng dụng.

```sql
-- trên destination PG15
CREATE ROLE app_user LOGIN PASSWORD 'AppStrongPass!' NOINHERIT;
CREATE DATABASE app_db OWNER app_user;

\c app_db

-- cài extension như source
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

Cấu trúc schema có thể dump từ source:

```bash
pg_dump -s -d app_db_src | psql -d app_db_dest
```

## Tạo subscription trên destination DB

```sql
-- trên PG15, vào đúng DB
\c app_db
CREATE SUBSCRIPTION app_sub
CONNECTION 'host=SRC_HOST port=5432 dbname=app_db user=repl_user password=StrongPass!'
PUBLICATION app_pub
WITH (copy_data = true, create_slot = true, enabled = true);
```

### **Giải thích nhanh**

- `copy_data=true`: để đồng bộ dữ liệu ban đầu.
- `create_slot=true`: sẽ tạo replication slot trên source để giữ WAL cho sub này.
- `enabled=true`: chạy ngay. Có thể để `false` rồi chạy copy chủ động nếu muốn kiểm soát.

## Theo dõi sync ban đầu

```sql
-- trên destination DB
SELECT subname, status, received_lsn, last_msg_send_time, last_msg_receipt_time, latest_end_lsn
FROM pg_stat_subscription;

-- trên source DB
SELECT slot_name, active, restart_lsn FROM pg_replication_slots;
SELECT application_name, state, sent_lsn, replay_lag FROM pg_stat_replication;
```

Khi copy xong, subscription chuyển sang trạng thái streaming. Lag về gần 0 là tốt.

## Đồng bộ các thay đổi không phải bảng

Logical replication không tự mang sequence, function, trigger, index partial đặc biệt, hay constraint mới. Cách làm an toàn:

Dump schema từ source sang destination định kỳ trước cutover:

```bash
pg_dump -s -d app_db_src | psql -d app_db_dest
```

Nếu có sequence, đồng bộ giá trị:

```sql
-- ví dụ
SELECT setval('schema_a.order_id_seq', (SELECT max(id) FROM schema_a.orders));
```

## Kế hoạch Cutover

### **Trình tự Cutover**

1. Kích hoạt **maintenance mode** hoặc **block writes** tại **application layer**.
2. Monitor **replication lag**, đảm bảo lag bằng 0 (đạt **zero lag**).
3. Thực thi **write-lock** trên **source database**.
4. **Disable subscription** để finalize state, sau đó thực hiện **data validation** (ví dụ: `row count`, `checksum`).
5. **Switch endpoint** của PgBouncer sang **destination DB**.
6. **Enable writes** trên destination database và **disable maintenance mode** tại application layer.

## Chi tiết các bước Cutover

### **Bước 1: Kích hoạt Maintenance Mode**

Ví dụ Rails:

```ruby
ApplicationSetting.current.update(maintenance_mode: true, maintenance_mode_message: "DB cutover, vài phút thôi")
```

Hoặc **block write requests** ở routes. Đảm bảo các background jobs dừng ghi mới trong **cutover window**.

### **Bước 2: Monitor Replication Lag**

```sql
-- destination DB
SELECT pg_sleep(1)
FROM generate_series(1,60)
WHERE (SELECT COALESCE(SUM(lag),0)
       FROM (SELECT EXTRACT(EPOCH FROM (now() - last_msg_send_time)) AS lag
             FROM pg_stat_subscription) t) = 0;
```

### **Bước 3: Thực thi Write-Lock trên Source DB**

Tùy vào application. Phương án là revoke quyền write của role `app_user` trong cutover window:

```sql
-- trên source DB
REVOKE INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public FROM app_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE INSERT, UPDATE, DELETE ON TABLES FROM app_user;
```

Hoặc dùng `pg_terminate_backend` với filter `app_name` để terminate các session ghi đang mở, nhưng cần thực hiện cẩn trọng.

### **Bước 4: Finalize State và Data Validation**

Disable subscription trong 10 đến 20 giây để finalize data state.

```sql
-- destination DB
ALTER SUBSCRIPTION app_sub DISABLE;
```

Thực hiện validation số lượng record của các bảng quan trọng:

```sql
-- source DB
SELECT COUNT(*) FROM schema_a.orders WHERE created_at < now();

-- destination DB
SELECT COUNT(*) FROM schema_a.orders;
```

Có thể dùng checksum theo từng batch id hoặc theo cột `updated_at`. Với các bảng lớn, **validate** theo range của primary key.

### **Bước 5: Switch Endpoint PgBouncer**

Chỉnh `pgbouncer.ini` trỏ sang **destination host**:

```ini
[databases]
app_db = host=DEST_HOST port=5433 dbname=app_db user=app_user password=AppStrongPass!
```

Reload config PgBouncer:

```bash
pgbouncer -R /etc/pgbouncer/pgbouncer.ini
```

Kiểm tra:

```bash
psql "host=pgbouncer port=6432 dbname=app_db user=app_user" -c "select version();"
```

### **Bước 6: Enable Writes và Disable Maintenance**

```sql
-- trên source DB, cấp lại quyền nếu cần duy trì parallel operation (read-only)
GRANT INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_user;

-- trên destination DB, đảm bảo full privileges
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO app_user;
```

Tại application:

```ruby
ApplicationSetting.current.update(maintenance_mode: false)
```

## Parallel Operation

Có thể bật reverse logical replication từ destination về source để hỗ trợ **rollback plan**. Thực tế tôi thấy cũng có thể giữ nguyên source instance ở chế độ read-only vài ngày trước khi **cleanup**.

## Post-Cutover Verification

- Application read/write vận hành bình thường.
- Không có lỗi `unique violation`.
- **SQL p95 latency** không tăng đột ngột.
- Replication slot trên source instance có thể giữ thêm vài giờ rồi mới drop.

## Cleanup

```sql
-- destination DB
DROP SUBSCRIPTION IF EXISTS app_sub;

-- source DB
SELECT pg_drop_replication_slot('app_sub');
```

**Drop publication** khi chắc chắn không còn sử dụng:

```sql
DROP PUBLICATION IF EXISTS app_pub;
```

## Monitoring và Alerting

- **Subscription lag** (theo thời gian).
- **WAL disk usage** tăng đột biến.
- Lỗi của **apply worker** trên destination DB.
- **Row count mismatch** ở các bảng trọng yếu sau cutover.

## Fallback/Rollback Plan

### **Phương án A (Application-level issue)**

Switch PgBouncer endpoint về source DB và enable writes trên source DB.

Subscription vẫn còn thì dữ liệu mới từ source sẽ không tự động sync sang destination. Sau khi fix lỗi, có thể **re-sync** thủ công hoặc rebuild logical replication.

### **Phương án B (Critical destination DB issue)**

Switch endpoint về source DB.

Drop subscription và slot để tránh **WAL retention** không cần thiết trên source DB.

Giữ lại **destination instance** để **investigate**.

## Tối ưu Performance (Bulk Data Copy)

- Tăng `maintenance_work_mem`, `work_mem` trên destination DB cho giai đoạn **initial copy**.
- Tắt autovacuum cho các bảng đang copy và enable lại sau khi xong để tránh **I/O contention**.
- Tạo index sau khi **initial copy** hoàn tất nếu publication theo từng nhóm bảng.
- Giới hạn **batch size** để destination DB theo kịp.

## Kết luận

Cho đến hiện tại thì với tôi tôi thấy logical replication là phương pháp ít rủi ro nhất để nâng cấp phiên bản Postgres hoặc migrate server mà không gây ra significant service disruption. Cái yêu cầu cốt lõi là chuẩn bị kỹ schema và permissions, thực hiện initial data copy, **monitor lag** và thực hiện **cutover** đúng thời điểm, có **monitoring** và **fallback plan** rõ ràng. Nếu anh em có phương án hay hơn tốt hơn tôi xin được lắng nghe học hỏi.

## Highlight Text

- production
- Source:
- Destination:
- App:
- Monitoring:
- Tạo role và publication trên source
- Giải thích nhanh
- Trình tự Cutover
- maintenance mode
- block writes
- application layer
- replication lag
- zero lag
- write-lock
- source database
- Disable subscription
- data validation
- Switch endpoint
- destination DB
- Enable writes
- disable maintenance mode
- Bước 1: Kích hoạt Maintenance Mode
- block write requests
- cutover window
- Bước 2: Monitor Replication Lag
- Bước 3: Thực thi Write-Lock trên Source DB
- Bước 4: Finalize State và Data Validation
- validate
- Bước 5: Switch Endpoint PgBouncer
- destination host
- Bước 6: Enable Writes và Disable Maintenance
- rollback plan
- cleanup
- SQL p95 latency
- Drop publication
- Subscription lag
- WAL disk usage
- apply worker
- Row count mismatch
- Phương án A (Application-level issue)
- re-sync
- Phương án B (Critical destination DB issue)
- WAL retention
- destination instance
- investigate
- initial copy
- I/O contention
- batch size
- monitor lag
- cutover
- monitoring
- fallback plan

## Images

- ![748bc866-cd6c-4ded-9fbe-57855a85bd32](https://devops.vn/uploads/images/2025/11/748bc866-cd6c-4ded-9fbe-57855a85bd32.webp)  — alt: `748bc866-cd6c-4ded-9fbe-57855a85bd32`

## Code Blocks

```
wal_level = logical
max_wal_senders = 10
max_replication_slots = 10
```

```
host replication repl_user 192.168.1.0/24 scram-sha-256
host all          repl_user 192.168.1.0/24 scram-sha-256
```

```
-- trên PG14
CREATE ROLE repl_user WITH REPLICATION LOGIN PASSWORD 'StrongPass!';

-- nếu app schema là public hoặc custom, chọn đúng schema
-- khuyến nghị publication theo schema để kiểm soát
CREATE PUBLICATION app_pub FOR ALL TABLES;

-- hoặc
-- CREATE PUBLICATION app_pub FOR TABLE schema_a.*, schema_b.*;
```

```
-- trên destination PG15
CREATE ROLE app_user LOGIN PASSWORD 'AppStrongPass!' NOINHERIT;
CREATE DATABASE app_db OWNER app_user;

\c app_db

-- cài extension như source
CREATE EXTENSION IF NOT EXISTS pgcrypto;
```

```
pg_dump -s -d app_db_src | psql -d app_db_dest
```

```
-- trên PG15, vào đúng DB
\c app_db
CREATE SUBSCRIPTION app_sub
CONNECTION 'host=SRC_HOST port=5432 dbname=app_db user=repl_user password=StrongPass!'
PUBLICATION app_pub
WITH (copy_data = true, create_slot = true, enabled = true);
```

```
-- trên destination DB
SELECT subname, status, received_lsn, last_msg_send_time, last_msg_receipt_time, latest_end_lsn
FROM pg_stat_subscription;

-- trên source DB
SELECT slot_name, active, restart_lsn FROM pg_replication_slots;
SELECT application_name, state, sent_lsn, replay_lag FROM pg_stat_replication;
```

```
pg_dump -s -d app_db_src | psql -d app_db_dest
```

```
-- ví dụ
SELECT setval('schema_a.order_id_seq', (SELECT max(id) FROM schema_a.orders));
```

```
ApplicationSetting.current.update(maintenance_mode: true, maintenance_mode_message: "DB cutover, vài phút thôi")
```

```
-- destination DB
SELECT pg_sleep(1)
FROM generate_series(1,60)
WHERE (SELECT COALESCE(SUM(lag),0)
       FROM (SELECT EXTRACT(EPOCH FROM (now() - last_msg_send_time)) AS lag
             FROM pg_stat_subscription) t) = 0;
```

```
-- trên source DB
REVOKE INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public FROM app_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA public REVOKE INSERT, UPDATE, DELETE ON TABLES FROM app_user;
```

```
-- destination DB
ALTER SUBSCRIPTION app_sub DISABLE;
```

```
-- source DB
SELECT COUNT(*) FROM schema_a.orders WHERE created_at < now();

-- destination DB
SELECT COUNT(*) FROM schema_a.orders;
```

```
[databases]
app_db = host=DEST_HOST port=5433 dbname=app_db user=app_user password=AppStrongPass!
```

```
pgbouncer -R /etc/pgbouncer/pgbouncer.ini
```

```
psql "host=pgbouncer port=6432 dbname=app_db user=app_user" -c "select version();"
```

```
-- trên source DB, cấp lại quyền nếu cần duy trì parallel operation (read-only)
GRANT INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_user;

-- trên destination DB, đảm bảo full privileges
GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO app_user;
```

```
ApplicationSetting.current.update(maintenance_mode: false)
```

```
-- destination DB
DROP SUBSCRIPTION IF EXISTS app_sub;

-- source DB
SELECT pg_drop_replication_slot('app_sub');
```

```
DROP PUBLICATION IF EXISTS app_pub;
```

## Tables

_(none)_

## Internal Links

- [https://devops.vn/expert-insights/ung-dung-ai-trong-devops-liam-nguyen/](https://devops.vn/expert-insights/ung-dung-ai-trong-devops-liam-nguyen/)
- [https://devops.vn](https://devops.vn)
- [Hỗ trợ?](https://devops.vn/support/)
- [Đăng nhập](https://devops.vn/login)
- [Đăng ký](https://devops.vn/signup)
- [Series](https://devops.vn/tag/devops-series/)
- [Mindset](https://devops.vn/tag/mindset/)
- [Linux](https://devops.vn/tag/linux/)
- [Bash](https://devops.vn/tag/bash/)
- [Docker](https://devops.vn/tag/docker/)
- [Kubernetes](https://devops.vn/tag/kubernetes/)
- [IaC](https://devops.vn/tag/infrastructure-as-code/)
- [Security](https://devops.vn/tag/security/)
- [Networking](https://devops.vn/tag/networking/)
- [Cloud](https://devops.vn/tag/cloud/)
- [Monitoring](https://devops.vn/tag/monitoring/)
- [Certificate](https://devops.vn/tag/certificate/)
- [AI](https://devops.vn/tag/ai/)
- [Tài nguyên](https://devops.vn/category/posts/)
- [Newsletter](https://devops.vn/newsletter/)
- [Giới thiệu](https://devops.vn/about/)
- [Blog](https://devops.vn/knowledge/)
- [Flashcards](https://devops.vn/flashcards/)
- [Cheat sheets](https://devops.vn/cheat-sheets/)
- [Sự kiện](https://devops.vn/events/)
- [Tin tức](https://devops.vn/news/)
- [Dành cho doanh nghiệp](https://devops.vn/vi/case-studies/)
- [Hữu Đoàn Đức](https://devops.vn/author/huudoanduc/)
- [10/11/2025](https://devops.vn/2025/11/10/)
- [Case Study](https://devops.vn/tag/case-study/)
- [Database](https://devops.vn/tag/database/)
- [Database Migration](https://devops.vn/tag/database-migration/)
- [PostgreSQL](https://devops.vn/tag/postgresql/)
- [Use case](https://devops.vn/tag/use-case/)
- [Zero Downtime](https://devops.vn/tag/zero-downtime/)
- [https://devops.vn/event/devops-vietnam-x-linux-foundation/](https://devops.vn/event/devops-vietnam-x-linux-foundation/)
- [https://devops.vn/event/state-of-devops-vietnam-2026/](https://devops.vn/event/state-of-devops-vietnam-2026/)
- [https://devops.vn/new/gap-go-ngo-tan-tai-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-python/](https://devops.vn/new/gap-go-ngo-tan-tai-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-python/)
- [https://devops.vn/new/gap-go-tran-quoc-toan-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-reactjs/](https://devops.vn/new/gap-go-tran-quoc-toan-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-reactjs/)
- [https://devops.vn/new/gap-go-trinh-the-long-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-java/](https://devops.vn/new/gap-go-trinh-the-long-chuyen-gia-dat-top-2-dockerfile-contest-2025-du-an-java/)
- [https://devops.vn/new/gap-go-nguyen-huu-phuong-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-reactjs/](https://devops.vn/new/gap-go-nguyen-huu-phuong-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-reactjs/)
- [https://devops.vn/new/gap-go-huynh-quang-tien-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-java/](https://devops.vn/new/gap-go-huynh-quang-tien-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-java/)
- [https://devops.vn/new/gap-go-nguyen-phuc-bao-lam-vo-dich-dockerfile-contest-2025-docker-image-nho-nhat/](https://devops.vn/new/gap-go-nguyen-phuc-bao-lam-vo-dich-dockerfile-contest-2025-docker-image-nho-nhat/)
- [https://devops.vn/new/gap-go-nguyen-the-thanh-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-python/](https://devops.vn/new/gap-go-nguyen-the-thanh-chuyen-gia-dat-top-1-dockerfile-contest-2025-du-an-python/)
- [DevOps VietNam](https://devops.vn/)
- [https://devops.vn/feed](https://devops.vn/feed)
- [Hỗ trợ](https://devops.vn/support)
- [Ebooks](https://devops.vn/ebooks)
- [Điều khoản sử dụng](https://devops.vn/terms/)
- [Chính sách bảo mật](https://devops.vn/privacy/)
- [https://devops.vn/event/webinar-lo-trinh-tro-thanh-devops-engineer-thuc-te/](https://devops.vn/event/webinar-lo-trinh-tro-thanh-devops-engineer-thuc-te/)
- [https://devops.vn/posts/tai-sao-ufw-vo-tac-dung-voi-docker/](https://devops.vn/posts/tai-sao-ufw-vo-tac-dung-voi-docker/)
- [https://devops.vn/posts/memory-con-trong-nhung-kubernetes-container-van-oomkilled/](https://devops.vn/posts/memory-con-trong-nhung-kubernetes-container-van-oomkilled/)

## External Links

- [https://www.facebook.com/devopsvietnam.fanpage](https://www.facebook.com/devopsvietnam.fanpage)
- [https://www.tiktok.com/@devops.vn](https://www.tiktok.com/@devops.vn)
- [https://www.instagram.com/devops.vn/](https://www.instagram.com/devops.vn/)
- [https://www.linkedin.com/company/devopsvietnam](https://www.linkedin.com/company/devopsvietnam)
- [Email](mailto:support@devops.vn)
- [https://www.dmca.com/Protection/Status.aspx?ID=dd363dc3-580e-4a61-8f48-7b41fb97cf39](https://www.dmca.com/Protection/Status.aspx?ID=dd363dc3-580e-4a61-8f48-7b41fb97cf39)
- [https://transparencyreport.google.com/safe-browsing/search?url=devops.vn](https://transparencyreport.google.com/safe-browsing/search?url=devops.vn)
