# Nginx subdirectory as root for React

## Metadata

- **URL:** https://devopsvn.tech/devops/nginx-subdirectory-as-root-for-react
- **Title:** Nginx subdirectory as root for React

## Headings

- H1: Nginx subdirectory as root for React

## Content

![Nginx subdirectory as root for React](https://images.unsplash.com/photo-1482062364825-616fd23b8fc1?ixlib=rb-4.0.3&q=90&fm=jpg&crop=entropy&cs=srgb&w=3840)

![Nginx subdirectory as root for React](https://app.notion.com/icons/priority-mid_gray.svg)

# Nginx subdirectory as root for React

Ngày đăng

May 5, 2024 8:12 AM

Tác giả

![](https://lh3.googleusercontent.com/a-/AOh14GgQbiQWTwvWr9UNj32QaIqsed4xULOmoWj58imk=s100)Quân Huỳnh

![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/10989452-4572-4f2a-b3f6-c46ee33c32b9/Bi_ng_FB/w=1920,quality=90,fit=scale-down)

Opening `package.json` and changing homepage property.

Copy

```
{
  "name": "devopsvn",
  "homepage": "/foo"
  ...
}
```

Opening `.env` and changing REACT\_APP\_PUBLIC\_URL with prefix path.

Copy

```
REACT_APP_PUBLIC_URL=/foo
```

Updating `nginx.conf`

Copy

```
server {
  listen 80;
  root /usr/share/nginx/html;

  location / {
    absolute_redirect off;
    return 301 /foo$request_uri;
  }

  location /foo/ { # end with /
    try_files $uri $uri/ /foo/index.html;
  }
}
```

Change `Dockerfile`

Copy

```
FROM node:20-alpine as build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:stable-alpine
# Update here
COPY --from=build /app/build /usr/share/nginx/html/foo
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

**First AI Journey for DevOps:**

1. [The AIOps Book](https://leanpub.com/the-aiops-book)

## Highlighted / Emphasized Text

- First AI Journey for DevOps:

## Images

- ![Nginx subdirectory as root for React](https://images.unsplash.com/photo-1482062364825-616fd23b8fc1?ixlib=rb-4.0.3&q=90&fm=jpg&crop=entropy&cs=srgb&w=3840) — alt: "Nginx subdirectory as root for React"
- ![Nginx subdirectory as root for React](https://app.notion.com/icons/priority-mid_gray.svg) — alt: "Nginx subdirectory as root for React"
- ![](https://lh3.googleusercontent.com/a-/AOh14GgQbiQWTwvWr9UNj32QaIqsed4xULOmoWj58imk=s100) — alt: ""
- ![image](https://images.spr.so/cdn-cgi/imagedelivery/j42No7y-dcokJuNgXeA0ig/10989452-4572-4f2a-b3f6-c46ee33c32b9/Bi_ng_FB/w=1920,quality=90,fit=scale-down) — alt: "image"

## Code Blocks

```none
{
  "name": "devopsvn",
  "homepage": "/foo"
  ...
}
```

```none
REACT_APP_PUBLIC_URL=/foo
```

```none
server {
  listen 80;
  root /usr/share/nginx/html;

  location / {
    absolute_redirect off;
    return 301 /foo$request_uri;
  }

  location /foo/ { # end with /
    try_files $uri $uri/ /foo/index.html;
  }
}
```

```none
FROM node:20-alpine as build

WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

FROM nginx:stable-alpine
# Update here
COPY --from=build /app/build /usr/share/nginx/html/foo
COPY nginx.conf /etc/nginx/nginx.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

## Tables

_(none)_

## Internal Links

_(none)_

## External Links

- [The AIOps Book](https://leanpub.com/the-aiops-book)
